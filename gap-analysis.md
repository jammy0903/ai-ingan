# AI인간 — 부족분 분석 & 단계별 구현 플랜

> 2026-06-18 작성. 방법: **3중 갭** — ①장르 retention 기둥(심리학) → ②cells·거지키우기 구현 방식 → ③우리 *실제 코드* 상태.
> 설계서(CLAUDE.md)가 아니라 **돌아가는 코드**(index.html / data.js / balance.js 전수조사) 기준.
> ⚠️ 외부 웹검색(firecrawl) 크레딧 소진으로 출처 인용은 보류 — 코드 실측 + 장르 구조 지식 기반. 다음 세션 재시작 후 firecrawl로 근거 보강 예정.

---

## 1. 한눈 비교표

| 장르 retention 기둥 | cells / 거지키우기 | **우리 실제 코드** | 갭 |
|---|---|---|---|
| **지수 숫자 폭발**(엔진②) | 1e15까지, ×10·×100 점프 | `rate` 1.0 시작, gen 0.35~3.4, **MAX_LV=10**, 비용 ×1.45 | 🔴 치명 |
| **무한/방대 콘텐츠** | 수십 era, 수백 스테이지 | **38노드(감정27+몸11) × 대사 3줄** | 🔴 치명 |
| **탭 손맛 성장** | 탭 업글·크리·콤보·골든셀 | `TAP_GAIN=1` 고정, **탭 업그레이드 0** | 🔴 치명 |
| **프레스티지 spend-tree** | 메타비트/환생보너스 *상점 트리* | depth=emo+body, **소비처는 재해석로그(2)뿐** | 🔴 |
| **변동보상(스키너)** | 골든셀·랜덤이벤트·가챠 | **전부 결정론적** | 🟠 |
| **일일 복귀 훅** | 일일미션·로그인보상·스트릭 | 오프라인 적립(캡 4h)**만** | 🟠 |
| **자동화 판타지** | 오토메이터 구매 | senses 폐기, person/body gen만 | 🟠 |
| **게임필(juice)** | 사운드·햅틱·코인 난무 | 파티클+숫자팝업만, **사운드/진동/쉐이크 0** | 🟠 |
| **소셜/바이럴** | 리더보드·공유 | Supabase 랭킹 언급, **UI 미구현** | 🟡 |
| **목표 사다리** | 업적·마일스톤·다음비용 노출 | 안개 빈 원만, **업적/마일스톤 0** | 🟡 |

---

## 2. 🔴 치명 3개 — 구조 자체의 문제

**① number-go-up이 빈혈이다.** `rate=1.0` 시작 + 노드 gen(0.35~3.4) **덧셈** + MAX_LV=10 + 비용 ×1.45 → 최대 rate가 수십걸음/초에서 천장. cells의 쾌감은 숫자가 **자릿수째로 터지는 것**인데 우린 산술적 덧셈이라 폭발이 없다. **방치형 도파민 엔진②를 안 켰다.**

**② 콘텐츠 유한 + LLM은 stub.** 노드 38개 × `lines[]` **3줄**. 재회 MAX_LV=10인데 `lineIdx=min(lv-1, lines.length-1)` → **재회 4회차부터 새 대사 0**(스탯만). 38노드는 30~60분 소진 = CLAUDE.md §3가 "실패 1순위"라 명시한 **중반 사막**에 정확히 빠진다. 이를 막을 LLM 풀은 `addSysLogs()` 훅만 있고 **실제 생성 로직 없음**. → 최대 자랑(서사로 사막 메우기)이 코드상 비어 있다.

**③ 탭이 60분 뒤 쓸모 0.** `TAP_GAIN=1` 고정, 탭 업글·크리·콤보 전무. idle이 수십/초로 가는데 탭은 영원히 +1 → 무의미해짐. **소정님 핵심 요구("cells처럼 누르는 손맛=핵심 가속")를 코드가 정면 배신.**

## 3. 🟠 중대 4개
- **④ 프레스티지에 쓸 곳 없음.** depth는 잘 정산되나(첫 생 38) 소비처가 재해석로그(LOOKBACK_COST=2)뿐. **메타통화로 영구배율 사는 상점 트리가 없다** = 회차 도는 기계적 동기 부재.
- **⑤ 변동보상 0.** 모든 만남 결정론적. 최강 훅 변동비율 강화 전무. (§4 금기는 "*이야기*를 결제 뒤에 가두지 마라"지 놀라움 금지가 아님 → 서사 훼손 없이 랜덤 발견 넣을 여지 있음.)
- **⑥ 일일 복귀 훅 = 오프라인뿐.** 일일미션·스트릭·시간상자 전무. 캡 4h도 짧음. "특정 시간에 다시 와라" 약속 메커닉 없음.
- **⑦ 자동화 판타지 약함.** senses 폐기로 자동화 성장 축 빈약. 살 수 있는 오토 계단 없음.

## 4. 🟡 보조 3개
- **게임필**: 사운드·음악·햅틱·화면쉐이크 0. 탭 중심 모바일 게임인데 청·촉각 피드백 부재.
- **소셜/바이럴**: "일기 공유"가 떡밥이라면서 공유·리더보드 UI 미구현.
- **목표 사다리**: 업적/마일스톤/진행바 부재.

## 5. 심리학 렌즈 압축
- **가진 훅**: 호기심(안개 빈 원), 손실회피(오프라인 캡), 서사 정당화(우리 해자), 완결감(노드 끝).
- **빠뜨린 훅**: 변동비율 강화(스키너), 목표 근접성/진행바, 숫자 자릿수 쾌감, 약속 메커닉(appointment), 자동화 통제감, 소셜 비교.

---

## 6. 단계별 구현 로드맵

> 규칙 준수: **리팩토링 먼저→기능 나중**, 작은 커밋 단위, 본문 로직 추측성 변경 금지, 프로젝트 스타일 유지(인라인+data.js+balance.js), 오버엔지니어링 금지.
> ⚠️ **SW 캐시 규칙**: `data.js`/`balance.js`/`index.html` 수정 시마다 `sw.js`의 `CACHE="aingan-vN"` 범프(안 하면 cache-first로 stale 서빙).
> 우선순위 근거: ②③이 *이미 만든 코어가 1시간 뒤 무너지는* 문제 + ③은 핵심 요구 위반이라 최상.

### Phase 0 — 리팩토링 솔기(behavior 불변, `refactor:`)
선행 작업. 이후 기능들이 깨끗이 얹히게 *계산 한 곳* 을 만든다.
- **0.1 유효치 게터 추출** — `S.rate`를 직접 변형하는 대신 `effRate()`/`effTap()`로 "기본값 × 메타배율" 계산을 한 곳에 모은다. 지금은 곱할 배율이 없으니 결과 동일(검증: 수치 불변). Phase 1·3·4가 여기에 배율만 꽂으면 됨.
  - 파일: `index.html`(rate/tap 계산부). 커밋: `refactor: 유효 산출/탭 계산을 effRate·effTap 게터로 분리`
- **0.2 콘텐츠 주입 솔기** — 사람/몸 노드 대사용 `addLines(key, tier, text[])` + 풀 구조를 `addSysLogs()` 패턴 그대로 추가(아직 빈 풀). Phase 2·5가 여기에 콘텐츠만 부어 넣음.
  - 파일: `index.html` + `data.js`. 커밋: `refactor: 노드 대사 주입 훅(addLines) 솔기 추가`

### Phase 1 — 탭 손맛(Fix③, `feat:`) ★최우선
- **1.1 탭=rate 비례.** `effTap()`가 `max(1, rate*TAP_FRAC)` 반환. `balance.js`에 `TAP_FRAC`(초안 0.15) 추가. → 후반에도 탭이 산다.
- **1.2 콤보 미터(juice).** 빠른 연타 시 콤보 누적·감쇠(`COMBO_STEP`/`COMBO_MAX`/`COMBO_DECAY`), 화면에 콤보 표시. 변동보상(⑤) 일부도 여기서 흡수(가끔 크리 탭).
  - 파일: `balance.js`+`index.html`(탭 핸들러). SW 범프. 커밋 2개로 분할(비례 / 콤보).

### Phase 2 — 콘텐츠 깊이(Fix② part a, `content:`)
- **2.1 대사 티어 확장.** `data.js`의 `lines[]`를 3 → 5~6단으로(MAX_LV와 정합). 재회가 4회차 이후에도 새 텍스트를 주게.
- **2.2 편지 트랙.** 깊은 재회는 그 사람의 **편지 한 조각**(별도 `letter[]`)을 추가 해금 = 서사 두 번째 축. 0.2 솔기 사용.
  - 파일: 주로 `data.js`(콘텐츠). SW 범프. 커밋: 노드 묶음별로 분할 가능.

### Phase 3 — 숫자 재튜닝(Fix①, `balance:`)
> 📚 **근거(2026-06-18, crawl4ai로 cells 위키·Steam 공략 실측)**: cells는 rate를 선형으로 안 올린다. 전부 **이산 마일스톤에서 배율 점프**:
> - "제너레이터를 다음 **랭크**로 올릴 때마다 산출 **×3**." / 천체 랭크업 ×3(트레잇까지 ×30) / 메조조익 랭크마다 메인 +1%, Lv50→프레스티지.
> - **프레스티지마다 +50% 영구 산출, 최대 10회.** (거지키우기도 동일 — 레벨 게이트 + 환생보너스 영구배율.)
> → 숫자 폭발 = "랭크/마일스톤 돌파 = ×배율"의 누적이지, 우리 `rate += gen` 선형 덧셈이 아님.
> 출처: cell-to-singularity.fandom.com/wiki/Help_for_Beginners, steamcommunity.com/sharedfiles/filedetails/?id=2983371927
>
> ✅ **결정: 옵션 ① 채택**(수집 마일스톤 배율만). 옵션 ②(노드 재회를 ×랭크로 전환, 전체 재밸런싱)는 추후 보류.

- **3.1 수집 마일스톤 배율 (옵션 ①, 구현 중).** (감정+몸) 수집 개수가 문턱[예 3·6·10·15·20·27·33·38]을 넘을 때마다 **전역 rate ×배율 점프.** Phase 0.1의 `rateMult()` 솔기에 곱 → idle·탭·HUD 일괄 반영(`rate += gen` 선형부는 안 건드려 세이브·밸런스 보존). HUD에 **"🎯 다음 속도업까지 N개"**(목표 근접성 훅) 노출.
  - 파일: `balance.js`(`MILESTONES`) + `index.html`(`rateMult`/`milestoneMult`/HUD) + `sw.js` 범프. 블래스트 반경: per-node `rate+=gen`·세이브 불변.
- **3.2 곡선 정튜닝.** `COST_MULT`/`UP_BASE`/`gen`/`MAX_LV`/문턱·배율 재조정 → "항상 거의 살 수 있는" cells식 긴장. **반드시 플레이로 검증**(추측성 금지).
- **3.3 (보류) 옵션 ② 노드 ×랭크.** 재회 레벨을 가산이 아닌 ×배율 랭크로(cells ×3 모사). 큰 재밸런싱이라 후순위.

### Phase 4 — 프레스티지 상점(Fix④, `feat:`)
- **4.1 마음의 깊이 메타 업글 트리.** depth로 사는 영구 강화(rate×, tap×, offline cap+, 시작 걸음+). 구매 레벨은 `S`에 프레스티지 보존. 적용은 **Phase 0 effRate/effTap 솔기**로 자동 반영.
  - 파일: `index.html`(프레스티지 UI)+`balance.js`(가격/효과). 커밋: 모델 / UI 분할.

### Phase 5 — LLM 풀(Fix② part b, 아키텍처 / 마지막)
- 클라이언트에 키를 둘 수 없으므로 **서버측 생성**(Supabase Edge Function → Claude) + 미리생성 캐싱 풀 + 풀 줄면 가끔 신선 호출. 0.2의 `addLines`/기존 `addSysLogs`가 주입구. **선(先) 정적 풀 확장(Phase 2)으로 사막 1차 방어 후** 착수.

### 백로그(별도 트랙, 추후)
- ⑤ 변동보상 본격화(길 위 랜덤 발견·희귀 재회·깜짝 색번짐) — §4 금기 회피 설계 필수.
- ⑥ 일일 복귀 훅(일일 한 줄 일기·스트릭·시간상자).
- ⑦ 게임필(사운드/햅틱/쉐이크) + ⑧ 소셜(리더보드·일기 공유) + ⑨ 목표 사다리(업적/진행바).

---

## 7. 권장 시작점
**Phase 0.1 → Phase 1**(탭 손맛). 가장 작고, 가장 핵심 요구를 직접 살리며, effRate/effTap 솔기는 이후 3·4단계의 토대가 된다. 그다음 Phase 2(콘텐츠)로 사막 1차 방어.

---

# 8. Play Store 출시 관점 보강 (2026-06-19, v1.0.13 재실측)

> §1~7은 **"재미 코어"**(첫 3분 손맛·숫자 폭발·콘텐츠) 기준. 이 절은 같은 코드를 **"비즈니스 코어"**(스토어에서 살아남고 돈 버는 KPI) 렌즈로 다시 본다.
> ⚠️ 재실측 노트: §1 표는 6/18 작성본이라 그 뒤 **완료된 Phase 1·2가 미반영**. 아래는 **현 코드(v1.0.13) 직접 grep 실측** 기준 — 이미 해결된 건 뺐다.
> **6/18 이후 해결됨(표에서 제외)**: 🔴탭 손맛(콤보×7·크리) · 🔴숫자 폭발(MILESTONES·K/M/B) · 콘텐츠 감정27 5단+pick.

## 8.1 비즈니스 KPI 갭표 (현 코드 실측)

| Play Store KPI | 표준 장치 | **우리 실제 코드 (grep 실측)** | 갭 |
|---|---|---|---|
| **ARPDAU·LTV**(매출) | rewarded ad·IAP | `admob/iap/billing/광고/결제` **매치 0** | 🔴 치명 |
| **D1/D7 리텐션** | 일일미션·스트릭·푸시 | `daily/streak/로그인보상` **0**, 오프라인 캡 **4h**만(`OFFLINE_CAP_H=4`) | 🔴 치명 |
| **D7+·세션수**(사막) | 무한 콘텐츠 | LLM 풀 `addSysLogs()` **주입구만, 생성 0** · 몸11 아직 3줄 | 🔴 치명 |
| **장기 리텐션** | 프레스티지 spend-tree | depth 소비처 **lookback뿐**, 영구배율 상점 **0** | 🟠 |
| **K-factor·CAC**(바이럴) | 공유·리더보드 | `navigator.share/leaderboard` **0**(pick 개그 출구 없음) | 🟠 |
| **전환·체감품질** | 사운드·햅틱 | `Audio/vibrate/AudioContext` **0**(탭 게임인데 청·촉각 0) | 🟡 |
| **측정 자체** | 애널리틱스·리뷰유도 | `analytics/firebase/gtag(실제)/review prompt` **0** | 🟡(전제) |

## 8.2 구조적 위험 (한 줄)
재미 1차전(첫 3분)은 통과했으나, 스토어 생사를 가르는 **2차전(D1 리텐션)·3차전(수익화)에 코드가 출전조차 안 함.** 로드맵(§6)이 "재미 코어"에 집중돼 **"비즈니스 코어"(측정·리텐션·수익) 트랙이 통째로 후순위**로 밀린 게 최대 리스크.

## 8.3 출시 우선순위 (개정 — 출시 전·유저 0 단계 기준)

> ⚠️ **정오표(2026-06-19 개정)**: 아래 ❌초안 순서는 *운영 중 게임* 플레이북을 **출시 전·유저 0** 단계에 잘못 적용한 것. 핵심 오류 = (a)애널을 #1로 올림, (b)"기준선 못 채우면 #2·#3 막힘"이라는 게이트로 묶음, (c)팀이 §3에서 스스로 "**실패 1순위=중반 사막**"이라 못 박은 항목을 #5로 강등(비용≠우선순위), (d)UA·스토어 래핑 누락, (e)프라이버시 DNA(§7)와 충돌하는 GA4 선택. → **✅합성 순서로 대체.** 근거는 아래 8.3.1.

**❌ 초안(틀림 — 기록 보존용):** ①애널 ②리텐션 ③수익화 ④게임필 ⑤LLM풀+몸11 ⑥프레스티지상점 ⑦공유 — *유저 0인데 깔때기 측정/마개부터 깐 순서.*

**✅ 합성 순서(채택):**
| 순위 | 무엇 | 왜 이 단계에 먼저 | 비용 |
|---|---|---|---|
| **1** | **제품 좋게 + 사막 1차 방어** = 게임필(햅틱·사운드) + **콘텐츠 깊이(몸11 5단·재회 심화 대사·목표문장 엔진)** | 출시 전엔 *제품이 좋아야* 한다. 게임필=가성비 최고(별점·체감 동시), 콘텐츠 깊이=§3 자칭 1순위 리스크(사막) **싸게** 1차 방어 | 낮~중 |
| **2** | **TWA 래핑 + 스토어 등재** | "Play Store 관점"의 진짜 게이트 = *아직 스토어에 없음.* 콘텐츠 등급·리스팅·ASO 포함 | 중 |
| **3** | **쿠키리스 애널 + 이벤트** (게이트 없이 **계측만 켜둠**) | 첫 유저 들어올 때 눈 뜨고 있으려고 *미리* 깐다. 단 기준선으로 #4를 막지 않음 | 낮 |
| **4** | **리텐션 + 수익화** | **실제 트래픽이 생긴 뒤.** 재미있는 게임에 붙어야 효과 — 얇은 게임에 넛지/광고는 새는 양동이 최적화 | 중 |

> **사막 방어의 진짜 수단(중요)**: "사막 올려라"의 정답은 *LLM 서버 지금 짓기*(비싸고 위험)가 아니라 — **이미 설계된 백로그를 손으로 채우기**: ①몸11 콘텐츠 5단(감정27은 이미 5단) ②재회 심화 대사·편지 트랙 ③목표문장 엔진(미구현 설계, CLAUDE.md). LLM 풀(§6 Phase 5)은 그 **2차 방어**로 미룬다.

---

## 8.4 구현 플랜 — 애널리틱스 (**개정: #1 아님 → #3 "계측만 켜둠"**)

> 🔧 **개정 배너(2026-06-19)**: 이 플랜은 본디 #1이었으나 8.3 정오표대로 **#3로 강등**. 바뀐 점 = (1)**GA4→쿠키리스**(PostHog/Plausible류) (2)**동의 게이트 삭제**(쿠키리스는 PII·쿠키 0이라 동의배너 불필요 → 온보딩 마찰도 사라짐) (3)**"기준선으로 #4 막기" 게이트 삭제**(유저 0이면 기준선이 안 채워져 로드맵이 교착됨 — *계측만 미리 켜두고* 트래픽 오면 읽는다). Phase A의 `track()` 솔기·이벤트 후크는 **그대로 유효**.
> ⚠️ **프로젝트 함정 회피**: (a) 빌드·번들러 없음·`file://` 유지 → 외부 SDK는 `<script src>` 1개 + no-op 폴백. (b) **세이브 프라이버시 정책(CLAUDE.md §7)** = 둘러보기는 *아무것도 저장 안 함(휘발)*. 쿠키리스 provider는 이 철학과 정합 → 동의 게이트 없이도 PII 0. (c) `index.html` 수정 → **SW 버전 1.0.13→1.0.14 범프(3곳 동기화)**.

### Phase A — `track()` 솔기 (refactor, behavior 불변)
프로젝트가 이미 쓰는 "솔기 먼저" 패턴(`effRate`/`addSysLogs`)과 동일. **외부 호출 없는 no-op 싱크**부터 깔고, 코어 흐름에 이벤트만 박는다.
- **A.1** `index.html`에 얇은 래퍼 추가(엔진 인라인 유지):
  ```js
  // 📊 애널 솔기 — 지금은 no-op(콘솔만). Phase B에서 쿠키리스 provider 연결.
  function track(name, props){ try{ (window._sink||(()=>{}))(name, props||{}); }catch(_){} }  // 핫패스 throw 방어
  ```
  (쿠키리스라 동의 플래그 불필요. PII는 절대 props에 넣지 않음.)
- **A.2 퍼널 이벤트를 *실제 솔기*에 박기** (전부 기존 함수 — 신규 로직 0):

  | 이벤트 | 후크 위치(실측 함수) | 측정 의도 |
  |---|---|---|
  | `app_open` | `bootSave()` (L1718) | DAU·세션 시작 — ⚠️**호출 카디널리티 먼저 실측**: `onAuthStateChange`+`getSession`(L1734-35)이 `bootSave`를 중복 호출하면 DAU 부풀려짐 → 세션당 1회 가드 필요 |
  | `wake_done` | `finishWake()` (L1800) | 온보딩 1단 통과율 |
  | `quest_received` | `endSamScene()` (L1929) | 온보딩 2단(샘) 통과율 |
  | `first_emotion` | `tryUnlock()` (L1198), `learned===1` 분기 | **활성화 북극성** — ⚠️**둘러보기/로그인 분리 필수**: 익명은 새로고침=초기화라 매번 재발화(=활성화 아님 "또 처음") → props에 `anon:bool` 달아 *로그인 코호트만* 북극성으로 |
  | `meeting` | `tryUnlock()` person 분기 | 감정키·누적수 → 콘텐츠 소진 곡선 |
  | `reunion` | 재회 레벨업 지점 | 재방문 깊이 |
  | `milestone` | `rateMult` 점프 지점 | 숫자 폭발 도달 분포 |
  | `prestige` | `doPrestige()` (L1431) | 회차 전환율 + depth |
  | `login` | `onAuthChanged()` authUser 셋 | 둘러보기→로그인 전환(=세이브 정책 핵심 KPI) |
  | `session_end` | `visibilitychange`/`pagehide` | **세션 길이**(8.1 KPI인데 초안에 빠졌던 이벤트) |

  → 이 10개로 **온보딩 퍼널(open→wake→quest→first_emotion)** · **이탈 지점** · **세션 길이**가 보인다.
- **A.3 검증**: `node --check` + 브라우저에서 `window._sink=console.log` 임시 세팅 후 이벤트 발화 확인. 외부 전송 0이라 동작 불변.
- 커밋: `refactor: 애널 track() 솔기 + 퍼널 이벤트 9개 후크(no-op 싱크)` · **SW 1.0.14 범프**

### Phase B — 쿠키리스 provider 연결 (동의 게이트 없음)
- **B.1 provider = 쿠키리스(권장 PostHog 또는 Plausible)** — 쿠키·PII 0이라 **§7 프라이버시 DNA와 정합 + GDPR 동의배너 불필요**(온보딩 마찰 0). Firebase/GA4는 쿠키·동의배너·구글 애드테크라 *이 게임 철학과 충돌*해 탈락.
  - `<script async defer src="…">` + `window._sink=(n,p)=>posthog.capture(n,p)`. 로드 실패/오프라인이어도 A.1 폴백이 no-op이라 게임 무중단. **동의 토글·`#gate` 변경 없음.**
- 커밋: `feat: 쿠키리스 애널 연결(PII 0·동의배너 불필요)` · **SW 범프**

### Phase C — 대시보드/지표 정의 (코드 외, **게이트 아님**)
- provider 대시보드에 **온보딩 퍼널**(open→wake→quest→first_emotion, *로그인 코호트*) + **D1/D7 리텐션** + 세션 길이 카드 구성.
- ⚠️ **합격선은 참고용일 뿐 #4를 막는 게이트가 아니다**(유저 0이면 기준선이 안 채워져 로드맵 교착 — 초안의 실수). 계측은 *그냥 켜두고*, 트래픽이 쌓이면 그때 읽어 #4(리텐션·수익화)를 데이터로 조정한다.

> **블래스트 반경**: A는 인라인 추가뿐(엔진 로직·세이브·data.js 불변). B는 외부 스크립트 1개. **세이브 정책(`if(!authUser)return`)·둘러보기 휘발성·`#gate` 절대 불변.**

## 8.5 나머지 항목 스켈레톤 (합성 순서 기준)

> 합성 순서(8.3) 기준 위치: **게임필 = #1의 절반**(아래 "게임필"), **콘텐츠 깊이 = #1의 절반**(8.3 블록인용 참고 — 몸11 5단·재회 대사·목표문장, 주로 `data.js`+`index.html` 손작업), **리텐션·수익화 = #4(트래픽 생긴 뒤)**.

### 게임필 (`feat:`) — **합성 #1의 절반, 가성비 최고·가장 빠름**
- **햅틱**: 탭 핸들러(`addWalk` 인근)에 `navigator.vibrate?.(8)` 1줄 + 크리 시 강한 패턴. 미지원 브라우저 무해.
- **사운드**: WebAudio로 가벼운 탭 틱·만남 차임(에셋 없이 oscillator). `localStorage` 음소거 토글(로그인 유저), 둘러보기는 세션 한정.
- 후크: 탭/`tryUnlock`/크리 지점. `index.html`만 → **SW 범프.** 외부 의존 0이라 **즉시 착수 가능 = 다음 작업 후보 1순위.**

### 리텐션 훅 (`feat:`) — **#4(트래픽 생긴 뒤)**, D1 방어
- **2-a 연속출석 스트릭**: `S.lastDay`/`S.streak`를 세이브에 추가(`SAVE_VERSION` +1 마이그레이션, 기존 진행 보존 — `data.js` 무관). `bootSave()`에서 날짜 비교 → 끊기면 리셋, 이으면 +1.
- **2-b 일일 보상**: 스트릭 일수→소량 depth/걸음 부스트(서사 비파괴, §4 안전). 게이트 아닌 *선물*.
- **2-c 오프라인 캡 상향 검토**: `OFFLINE_CAP_H` 4→8(밤새 적립 = "자기 전 켜둘 이유"). 경제 영향이라 #3 계측으로 before/after 측정.
- **2-d (후속) 웹푸시**: PWA라 가능하나 권한·서버 필요 → 스트릭이 자리잡은 뒤. **로그인 유저 한정**(둘러보기는 휘발이라 푸시 대상 아님).
- 후크: 전부 `bootSave()`/`saveState()` 인근 + `balance.js` 상수. **SW 범프.**

### 수익화 (`feat:`) — **#4(트래픽 생긴 뒤)**, rewarded ad 1개부터
- **AdMob rewarded** 단 1슬롯: "오프라인 적립 ×2 받기" / "이번 ×N 가속" — **서사 무관 가속만**(§4 금기 = *이야기*를 가두지 마라 → 가속 보상은 안전).
- **첫 경험 광고 0**(CLAUDE.md 명시) → 광고 노출은 **first_emotion 이후**로 게이트(#3 이벤트 재사용).
- **IAP "광고 제거"** 1종부터. TWA/Play Billing은 래핑(8.3 #2) 단계와 함께.
- ⚠️ 웹(`file://`/PWA)에선 AdMob 직접 불가 → **Play 래핑(TWA) 시점에 배선**. 그 전엔 자리(보상 훅)만 #3 솔기로 비워둠.

> **다음 작업 후보**: 8.3 합성 #1 = **게임필(위, 즉시 착수)** → 콘텐츠 깊이(몸11 5단). LLM 풀·프레스티지 상점·공유는 §6 백로그로 위임. **계측(#3)이 켜진 뒤 트래픽 데이터로 #4(리텐션 vs 수익화)를 재검증**하는 게 핵심.
