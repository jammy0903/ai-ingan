# 경제/통화 모델 재설계 — "걷기 고정 + 성장은 탭" (확정·미구현)

> 확정일: 2026-06-22. 상태: **합의 완료, 미구현(코딩 전).**
> 한 줄: idle 성장 폭발(이중 지수)을 걷어내고 — **걷기(idle)는 끝까지 1걸음/초로 고정**,
> 숫자가 커지는 "성장(number-go-up)"은 **탭 파워**로 옮긴다. 그러면 노드 비용 `w`가
> "그 노드까지 걷는 초"로 그대로 읽혀(숫자 폭발 없음), 튜닝이 직관적이 된다.

---

## 0. 왜 바꾸나 (문제)

지금 구조는 **이중 지수**다: 걸음/초 = `baseRate(노드·재회) × milestoneMult`. 마일스톤만 누적 ×3.7만,
재회까지 곱해 income이 K/M/B로 폭발한다. 그 결과:

1. **숫자 폭발이 톤(수채화 잔잔)과 안 맞고**, 유저가 "너무 빠르다 / 후반 폭발"로 느낌.
2. 발견비용이 income을 추종(`cost = w × effTap/6`)해서 **`w`가 "시간"이 아니라 "탭 수"에 매핑** → `w` 숫자가 직관적이지 않음.
3. cost가 income을 따라가니 **숫자를 키워도 "하루 걸리는 게이트"가 안 만들어진다**(손빠른 유저는 탭으로 뚫음).

→ 유저 결정: **걷기 1/sec 고정 + 성장은 탭으로.** (대안: 그냥 idle 성장 유지 = 기각.)

---

## 1. 새 모델 (확정, 2026-06-22 — 숫자까지 확정)

- **걷기(idle) = 고정 1걸음/초, 끝까지.** 마일스톤·재회의 idle 가속 효과 폐기.
- **탭 파워 = 1 + (모은 감정 수) + (재회 레벨 합).** 감정 1개 줍기 = 탭 **+1**, 재회 1레벨 = 탭 **+1**. → 모을수록 탭이 세지는 number-go-up. idle은 잔잔한 바닥(1/초), 탭은 능동 가속기.
- **마일스톤(개수 문턱마다 ×점프) 제거.** 자동이 1 고정이라 곱할 데가 없음 → `MILESTONES`/`rateMult` 삭제. number-go-up은 *탭 파워 + 총 걸음(K/M 표기) + 재회 깊이 + 색 번짐*으로.
- **콤보·크리 = 변동 양념(유지).** 연타 콤보·크리(×6 팡!)는 그대로 손맛. 정확한 상한은 2단계에서 플레이로 미세조정.
- **발견비용 `reachCost = w` (W_MULT=1, effTap 추종 제거).** → `w` = **그 노드까지 걷는 초**. 화면에 그 숫자 그대로. 숫자 폭발 사라짐.
- **재회의 의미**: idle income 대신 *탭 파워(+1/레벨) + 온기 코인 + 더 깊은 대사*. "grind = 관계 깊어짐 = 탭 세짐" = §5-A 해자 유지.
- **오프라인 = 1걸음/초(온라인 idle과 동일), 누적 상한 10,000걸음.** 자고 오면 ~2.8시간치(만 걸음)까지 쌓임 = "쌓여있다" 보장 + 무한 방치 방지.

### 검산 (목표 A = 빡탭 1~2시간 / idle 하루)
탭 파워가 긍정 10개 줍는 동안 1→~10으로 자람(평균 ~5걸음/탭). 빡세게(초당 4탭) → 86,400걸음 ÷ (4×5) ≈ **1시간 12분**. 콤보·크리 타면 ~40분. 가만 두면 86,400초 = **하루**. → 목표 A 달성.
- 뒤 묶음일수록 탭 파워 누적(↑) → 더 빨리 깸 = 강해지는 보람. idle은 묶음당 하루 고정 유지.

---

## 2. 스케일 목표 + w 표 (4대분류 전부)

**목표(가만히 둘 때 = idle 고정 1걸음/초): 대분류 하나당 +1일씩.**
- 긍정 완주 → 강한자극 도착 = **1일**
- 강한자극 완주 → 불안·불편 도착 = **2일**
- 불안·불편 완주 → 잔잔·시림 도착 = **3일**
- 잔잔·시림 완주 → 몸/엔딩 = **4일**

기준: **1일 = 86,400걸음**(86,400초). idle 고정 1/sec이므로 **Σw = 순수 idle 초**.
각 대분류 감정 w 합 ≈ **86,400(=하루)**, 안에서는 앞 빠름→뒤 느림(티어 大→中→小). 열탭하면 그만큼 당겨짐.

**누적(걷기만 도착 시점):**
| 도착 | 누적 걸음 | 순수 idle |
|---|---|---|
| 강한자극(2번째) | ~89,400 | **1일** |
| 불안·불편(3번째) | ~175,800 | **2일** |
| 잔잔·시림(4번째) | ~262,200 | **3일** |
| 몸/엔딩 | ~348,600 | 4일 |

### 긍정·흥미 (10개 + 게이트 7,000 = 89,400 ≈ 1일)
| 감정 | 티어 | w | 순수 idle |
|---|---|---|---|
| 기쁨(joy) | 大 | 300 | 5분 |
| 애틋함(adore) | 中 | 800 | 13분 |
| 재미(amuse) | 中 | 1,800 | 30분 |
| 설렘(flutter) | 中 | 3,500 | 1시간 |
| 호기심(curious) | 小 | 6,000 | 1.7시간 |
| 아름다움(beauty) | 小 | 9,000 | 2.5시간 |
| 감탄(admire) | 小 | 12,000 | 3.3시간 |
| 경외(awe) | 小 | 15,000 | 4.2시간 |
| 황홀(trance) | 小 | 16,000 | 4.4시간 |
| 뿌듯함(satisfy) | 小 | 18,000 | 5시간 |
| **강한자극 진입(게이트)** | 분류 | 7,000 | 2시간 |

### 강한자극 (5개 = 86,400 ≈ 1일)
| 감정 | 티어 | w |
|---|---|---|
| 신남(excite) | 大 | 6,000 |
| 안도(relief) | 中 | 13,000 |
| 끌림(attract) | 中 | 18,000 |
| 갈망(crave) | 小 | 23,000 |
| 해냄(triumph) | 小 | 26,400 |

### 불안·불편 (7개 = 86,400 ≈ 1일)
| 감정 | 티어 | w |
|---|---|---|
| 두려움(fear) | 大 | 4,000 |
| 혼란(confuse) | 中 | 7,000 |
| 지루함(bored) | 中 | 9,400 |
| 민망함(awkward) | 中 | 12,000 |
| 불안(anxiety) | 小 | 15,000 |
| 혐오(disgust) | 小 | 18,000 |
| 오싹함(horror) | 小 | 21,000 |

### 잔잔·시림 (5개 = 86,400 ≈ 1일)
| 감정 | 티어 | w |
|---|---|---|
| 슬픔(sorrow) | 大 | 6,000 |
| 안쓰러움(empathy) | 中 | 13,000 |
| 그리움(longing) | 中 | 18,000 |
| 연민(compassion) | 小 | 23,400 |
| 평온(calm) | 小 | 26,000 |

- 첫 감정(기쁨 300) = idle 5분 / 탭하면 1분 내 → 초반 훅 유지.
- **각 대분류 시작은 일부러 싸게**(fear 4,000·sorrow 6,000…) = 새 챕터 진입 시 빠른 한 방 = 재훅. 안에서 다시 램프.
- **몸11(BODY)은 이 스케일 밖** — 그래프 노드에서 빼고 「온기 조각」으로 구매하는 방향(별도 설계, Stage 2 대기). 그래프 w 스케일링 대상 아님.

---

## 2.5 튜닝 기준선 (지수 ROI 자료 대비 압박 검증, 2026-06-23)

> 거지키우기·쿠키클리커류 "지수 income × 구간 부스터" 모델의 건강성 기준(**역전 없음 + 완만 우상향 + 벽 없음**)으로 §2 w표를 검증. 우리는 "타수" 대신 "걷는 초(w)" 도메인이라 **w / 산출**로 환산. ⚠️ 이 자료의 *공식*(지수 income·구간 부스터)은 우리가 6/22에 일부러 버린 이중 지수라 **적용 안 함** — *원리(역산·완만 우상향)만* 검증에 사용.
> 가정: idle 1/s, 탭파워 = 1 + 모은 감정수(+재회레벨; 아래는 보수적으로 재회 0), 빡탭 4탭/초, 콤보·크리 제외(=하한).

### 긍정·흥미 전체 (자료식 스프레드시트)
| # | 감정 | w | idle만 | 탭파워 | 빡탭 w/(1+4·탭) | 순수 탭수 w/탭 |
|---|---|---|---|---|---|---|
| 1 | 기쁨 | 300 | 5분 | 1 | 1.0분 | 300 |
| 2 | 애틋함 | 800 | 13분 | 2 | 1.5분 | 400 |
| 3 | 재미 | 1,800 | 30분 | 3 | 2.3분 | 600 |
| 4 | 설렘 | 3,500 | 58분 | 4 | 3.4분 | 875 |
| 5 | 호기심 | 6,000 | 1.7h | 5 | 4.8분 | 1,200 |
| 6 | 아름다움 | 9,000 | 2.5h | 6 | 6.0분 | 1,500 |
| 7 | 감탄 | 12,000 | 3.3h | 7 | 6.9분 | 1,714 |
| 8 | 경외 | 15,000 | 4.2h | 8 | 7.6분 | 1,875 |
| 9 | 황홀 | 16,000 | 4.4h | 9 | 7.2분 | 1,778 |
| 10 | 뿌듯함 | 18,000 | 5h | 10 | 7.3분 | 1,800 |

**판정 ✅**: 빡탭 시간 1분→7분 완만 우상향 후 평탄, 역전·벽 없음. 합계 빡탭 ≈ 48분(+게이트 ~3분) → "긍정 빡탭 ~1시간"과 일치.

### 대분류 진입 = 의도된 톱니(재훅)
| 진입 노드 | w | 그 시점 탭파워 | idle만 | 빡탭 |
|---|---|---|---|---|
| 신남(강한자극 첫) | 6,000 | ~13 | 1.7h | ~2분 |
| 두려움(불안 첫) | 4,000 | ~16 | 1.1h | ~1분 |
| 슬픔(잔잔 첫) | 6,000 | ~23 | 1.7h | ~1분 |

새 챕터가 idle·탭 둘 다 싸게 시작→안에서 재램프 → **방법 A "구간 부스터 단기목표"를 income×N 없이 톱니로 재현.** (삭제한 `MILESTONES` 자리를 이게 메움.)

### ⚠️ 명시 트레이드오프 — 후반 능동 탭 시간 단축 ("역전"의 약한 형태)
후반일수록 **빡탭(능동) 시간은 오히려 줄어듦.** 평온(w=26,000, 탭파워~40)=빡탭 ~2.7분 < 긍정 뿌듯함 7.3분.
- 이유: **탭파워는 누적 합(선형 4×)으로 크는데 w는 챕터당 1일에 묶여 ~1.4×밖에 안 큼.**
- **파괴적 역전 아님**: idle 척추(1일/챕터 게이트)는 그대로 살아있고 탭만 빨라짐 = 후반 권능감으로 작동. idle 시간은 계속 우상향 ✅.
- "**idle 1일/챕터**"와 "**탭 시간 평탄**"은 선형 탭파워에선 동시 불가(평온 탭 평탄화하려면 w~70k → idle 19h로 1일 게이트 붕괴). 문서는 **idle-day를 선택**했으니 후반 탭 단축은 그 대가.
- 레버(후반 탭도 뻑뻑하게 원할 시): (a) 탭파워를 합→√(개수)·로그로 완만화, 또는 (b) data.js w 후반 더 가파르게(idle 1일 일부 포기). **지금 설계 의도(권능감)면 손대지 말 것.**

### 콤보·크리
위 표는 콤보·크리 제외 **하한**. 콤보(최대 ×7)·크리(12%×6) 켜지면 실 빡탭 ≈ 위 값의 **절반 이하** + 변동 도파민. → **2단계(콤보/크리 완만화)에서 이 표를 기준선 삼아 묶음당 1~2시간에 맞춤.**

---

## 3. 구현 단계 (리팩토링 먼저, 단계별 검증·커밋)

> CLAUDE.md §0.1: 구조 변경 → 검증 → 커밋 → 그 다음 다음 단계. 섞지 말 것.

### 1단계 — 엔진 전환 (`index.html` · `balance.js`)
- idle 루프 = **고정 1걸음/초** (`S.walks += 0.1`; 마일스톤·재회 idle 가속 제거).
- `effRate`(idle용)=**1 고정** 과 `tapPower`(성장용)= **1 + 감정수 + 재회레벨합** 으로 **분리**. `effTap`은 `tapPower` 사용.
- **`MILESTONES`/`rateMult` 삭제** (idle·탭·오프라인·구름선물에서 곱 제거).
- `reachCost = w`, **effTap 추종(`scale`)·`W_MULT` 제거**(또는 `W_MULT=1` 상수화).
- 오프라인 = `1걸음/초`, 누적 **상한 10,000걸음**(`OFFLINE_CAP_STEPS=10000`; 옛 `OFFLINE_RATE`·`OFFLINE_CAP_H`는 이 단순 모델로 대체).
- HUD "걸음/초"=1 작게, 대신 **"탭 파워" + 총 걸음(K/M)** 노출.
- **블래스트 반경**: `effRate`/`effTap`/`rateMult`/`MILESTONES` 참조처 전부 — idle 루프 · HUD · 탭(`addWalk`) · 오프라인 적립 · 구름 선물(`effRate×10`) · 샘씬 idle정지 등.

### 2단계 — 탭 파워 완만화 (`balance.js`)
- 콤보 `COMBO_MAX`/`COMBO_STEP`·크리(`CRIT_CHANCE`/`CRIT_MULT`) 상한을 플레이로 미세조정 → 빡탭이 묶음당 ~1~2시간에 수렴.
- (마일스톤은 1단계에서 이미 삭제 — 탭 파워는 감정+재회 개수로만.)

### 3단계 — w 스케일 (`data.js`)
- §2 표대로 긍정 w 재설정. 이어서 나머지 대분류·몸 w도 일관 스케일(곡선 일관성).

---

## 4. 후속/미해결

- ~~오프라인 캡 재검토~~ → **확정(2026-06-22): 1걸음/초 · 누적 상한 10,000걸음.** 자고 오면 ~2.8시간치 보장.
- ~~나머지 대분류 w 스케일~~ → **§2에 4대분류 전부 채움(2026-06-22)**. 각 대분류 = 1일, 누적 1·2·3·4일. 몸11은 온기 구매로 빠짐(스케일 대상 외).
- **세이브 마이그레이션**: 누적 `S.rate` 등 레거시 필드와의 호환(`SAVE_VERSION` 범프 필요할 수 있음).
- **버전 범프 규칙**: data.js·balance.js 수정 시 `sw.js`·`manifest.json`·`#appVer` 3곳 동기화.
