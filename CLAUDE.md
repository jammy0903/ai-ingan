# AI인간 (aingan) — Agent Entry Guide & 게임 설계서

이 저장소의 모든 에이전트가 가장 먼저 읽는 파일. 프로젝트 정체성 + 분석 + 게임 설계를 한곳에 모은다.

---

## 0. WORKING RULES (read before touching anything)

> Rules are in English; talk to the user and write code comments in Korean.

### 0.0 Language
- **Always talk to the user in Korean.** No exceptions, regardless of the language the user writes in.

### 0.1 Refactoring rules
1. **Refactor first, add features second. Never both at once.** Finish the structural change, verify it works (test), commit — *then* layer the new feature on top. Mixing them makes bugs untraceable. Always split into stages.
2. **Don't change business logic on speculation.** No "this would be cleaner/faster" rewrites of existing data flow or error handling. Touch only what the user explicitly asked to change or add.
3. **Trust the code over the comments.** Comments may be decades-old fossils — analyze the logic that actually runs, not the comment beside it. While refactoring, delete or update stale/wrong comments so they match reality.
4. **Map the blast radius first, then report.** Before editing a shared/common function or anything referenced elsewhere, tell the user "this also affects A and B — proceed?" and wait. Never edit shared code blind.
5. **Keep changes splittable into small git commits.** Don't overturn dozens of files at once. Deliver incrementally — "Stage 1: extracted fn X", "Stage 2: added feature interface Y" — so the user can review and commit each separately.
6. **Stay in this project's style.** No forced latest-trend or heavyweight design patterns. Match the existing conventions, naming, and architecture grain. **No over-engineering.**

### 0.2 Commit & push rules
- **Read `git-rule.md`** before any git operation (commit / branch / staging). It is the source of truth for commit conventions.
- **Commit or push only when the user asks.** End the commit body with:
  `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### 0.3 New-session rules
- **Read `project-summary.md` first** to understand the project, then this file (CLAUDE.md) and the `MEMORY.md` index — together they are the source of truth for project state and prior decisions.
- **`git pull` all branches** at session start so local is in sync (`git fetch --all` + pull current branch).
- **Start from a clean `git status`** when beginning staged work; verify with `git status` before editing.
- No hot reload: after editing, the user must manually F5. Verify syntax with `node --check` on the inline script before claiming done.
- Check `plan.md` and recent commits for in-flight work before starting something new.

---

## 1. 프로젝트 정체

- **제목: AI인간** (aingan)
- **한 줄**: 버려진 고철 로봇(**AI**)이 끝없는 길을 여행하며 사람을 만나고 감정을 하나씩 배워 마침내 **인간**이 되는, 수채화풍 감성 방치형(idle) 게임.
- **왜 이 제목**: 게임 아크 자체가 "**AI → 인간**". 단순 워드플레이가 아니라 본질과 일치. 발음도 브랜드 *aingan*.
- **배포 도메인**: **`aingan.click` = AI인간 전용**(2026-06 인간사냥 폐기, 도메인·Supabase 프로젝트를 AI인간이 통째로 점유). 호스트는 정적(GitHub/Cloudflare Pages 등) + 커스텀 도메인.
- **독립 저장소**: 새 git(`/home/jammy/projects/ingan`). 폐기된 인간사냥(`projects/ani/demo/hunt`)과 코드·백엔드 모두 무관.
- **목표**: 독립 수익 게임(Play Store 등). 바이럴 + 광고/IAP 수익. 콜드스타트·LLM비용·재미 약점을 idle 구조로 해소.

---

## 2. 확정된 방향

- **장르**: 방치형(idle/incremental) + 탭(active). 실시간 멀티 netcode 없음 → 저위험.
- **아트 톤**: **수채화풍(잔잔) 확정.** → `mockup/tone-c-watercolor.html`
  - (탈락안 픽사풍도 참고 보관: `mockup/tone-a-pixar.html`)
- **정서**: 감동. 로봇의 **순진한 시선으로 감정을 처음 정의**하는 톤이 핵심 무기.
- **탭 비중**: 소정님 요청대로 "cells처럼 계속 누르는" 손맛 유지 → **탭 = 핵심 가속.**

---

## 3. 왜 방치형이 통하나 — 성공작 분석 요약

거지키우기 / Cell to Singularity / idle 일반(Cookie Clicker·Paperclips·Spaceplan 등) 분석 수렴 결과.

1. **이중 엔진** = "성장이 펼쳐지는 쾌감" + "다음 보상이 코앞이라는 기대". 산출·비용이 둘 다 지수 → 항상 "조금만 더"인 상태가 도파민 엔진.
2. **첫 3~5분이 생사를 가른다.** 즉시 숫자 상승 → 빠른 첫 업그레이드 → "안 눌러도 굴러간다"는 자동화 순간 조기 증명. (못 하면 즉시 이탈 = 방치형 1순위 실패)
3. **프레스티지(환생) = 스스로 택한 리셋을 영구 배율로 보상.** 리셋은 좌천이 아니라 **졸업**. cells는 서사로 정당화(시뮬 재부팅), 거지키우기는 레벨 게이트+영구 환생보너스.
4. **"다음 노드"가 항상 흐릿하게 보여야 한다** (cells의 빈 원·흰 선). 호기심이 연료.
5. **오프라인 진행 + 상한(cap) + "돌아오니 쌓여있다" 트리거.** 무제한이면 돌아올 이유 없음. 캡+푸시가 손실회피로 재접속 유도.
6. **성장은 숫자가 아니라 "변화"로 체감된다.** 화면이 안 변하면 성장감이 죽음(새 요소 등장·UI 변형·색 변화).
7. **서사가 숫자를 정당화한다.** "왜 이 클릭을 하나"에 답이 있으면 같은 클릭이 의미를 얻음.

> **실패 1순위 = 중반 콘텐츠 사막.** 초반 몰입 다 쓰고 중반이 비면 다 떠난다.

---

## 4. 우리만의 무기 + 절대 금기

> 일반 방치형은 차트가 비면 지루해진다. **AI인간은 빈 차트를 "서사 빈칸"으로 채운다.** 통화를 "골드"가 아니라 **감정·기억·사람다움**으로 치환하면 — *숫자가 오르는 것 = 이야기가 열리는 것*, *리셋(다시 걷기) = 서사의 필연*, *복귀 = 재회*가 된다. 방치형 최대 약점(중반 사막)이 우리에겐 해자.

🚫 **절대 금기: 다음 감정/이야기 한 줄을 광고·결제 뒤에 가두지 말 것.** 감동을 돈으로 막으면 끝.

---

## 5. 핵심 루프 (게임 설계)

### 스토리 캐넌 (확정 — 구현됨)

> **한 줄 아크**: 고철 로봇(AI)이 고철 더미에서 깨어나 **가슴(코어)이 텅 빈 결핍(고독)** 을 느낀다. 창조주 「샘 알트머스크」와 동기화를 시도하다 실패(`[ERROR: 창조주 동기화 실패]`)하지만 — 이름을 얻고 길로 나서자, **길 위에 샘이 사람으로 나타나** 알려준다: *"넌 아직 사람이 아니야. 사람이 되려면 마음(감정) 27 + 몸(기관계) 11을 전부 모아와. 그럼 짠— 하고 진짜 사람으로 만들어줄게."* 로봇은 끝없는 길을 걸으며 **감정과 몸을 '병렬'로(동시에)** 하나씩 모은다(감정 근거: **Cowen & Keltner, 2017 — 인간 감정 27가지**). 둘 다 모아 **길 끝에서 샘과 재회** → 샘이 마법을 부리려다… **아무 일도 안 일어난다. "뭐 해? 어차피 넌 벌써 사람인데."** → **반전: 모으는 과정(슬픔·기쁨·그리움을 안 그 순간들) 자체가 이미 사람이 되는 거였다.** 기쁨+상실을 동시에 느끼며 운다(= "고장이 아니다").
>
> ⚠️ **2026-06 캐넌 변경(구현 완료)**: (1) 옛 "샘은 길 끝에서만 재회 / 감정27 다 모은 뒤 몸 챕터 순차" → **샘은 온보딩(시작)에서 퀘스트를 주고 끝에서 재회(반전)**, **감정27 ↔ 몸11**(⚠️ 2026-06-18 v1.0.11~12 재변경: 옛 '병렬' → **감정 먼저(대분류 순차) → 몸 맨 마지막** 으로 회귀). (2) 엔딩은 "샘이 만들어줌"이 아니라 **"넌 이미 사람이었다" 반전**.
>
> - **그 사람 = 샘 알트머스크** — 나를 만든 이이자 사람으로 만들어 줄 이(AI 거물 풍자 + 창조주의 뭉클). **온보딩(시작 퀘스트 부여)에 등장 + 최종 노드(재회·반전)** 양쪽에 있음.
> - **수집 = 감정 27 + 몸 11** (⚠️ v1.0.11~12: **병렬 → 순차** — 감정 4대분류 순차 완성 후 **몸이 맨 마지막**으로 열림) — 각 만남(person 노드)에서 감정 1조각 + **그 감정을 스스로 곱씹는 mem 한 줄**('그 사람'/창조주 연모 서사는 제거됨); 몸(body 노드)은 감정27 완성 뒤 `start`에서 뻗는 가지로 성장. 하늘 HUD `🧩 감정 N/27` + `🫀 몸 N/11`(둘 다 모으면 샘 재회).
> - **감정 27 = Cowen & Keltner(2017) 학술 목록에 충실** (구현 완료, 27/27). 따뜻한 감정뿐 아니라 **불편·복잡한 감정까지** 포함 — 혐오·오싹함·지루함·혼란·갈망 등. 로봇의 *순진한 재정의*는 이런 날선 감정에서 더 빛난다는 판단.
>   - ⚠️ **2026-06-16 구조 개편(구현 완료)**: 옛 'start→6갈래' 평면 트리를 **root(감정) → 4 대분류 → 세부 감정** 3계층 그래프로 교체. 4 대분류 = **긍정·흥미(10: 애틋함·기쁨·재미·설렘·호기심·아름다움·감탄·경외·황홀·뿌듯함) / 불안·불편(7: 두려움·혼란·지루함·민망함·불안·혐오·오싹함) / 강한 자극(5: 신남·안도·끌림·갈망·해냄) / 잔잔·시림(5: 슬픔·안쓰러움·그리움·연민·평온)**. 각 분류는 걸음으로 닿는 중간 허브 노드(`type:"category"`)이고 그 아래 감정이 펼쳐진다. 추가로 **연관/반대 '시각 전용' 엣지**(`REL_EDGES`: assoc=강도축·이웃 / oppo=거울쌍)로 감정 그라데이션을 지형화 — 이동·발견엔 영향 없음. 엔딩은 여전히 **감정27 + 몸11 전부 수집 = 샘 재회**(해냄 게이트 아님). 상세 계획 = `emotion-graph-redesign.md`.
>   - ⚠️ 캐넌 변경: 옛 휴먼드라마 로스터의 **사랑·용기·용서는 학술 27에 없어 제외**(엔딩은 자부심 대신 **해냄(Triumph)** 으로 재연결). **성적 욕망(#25)은 '끌림(연정)'으로 순화**해 SFW로 포함.
>   - 🚫 **감각(빗소리·바람·햇빛·별빛)은 폐기** — 감정 노드를 흐리는 비서사 부품이라 제거. 보조 생성기 없이 **사람 노드 재회만으로** 걸음/초를 키운다.
> - **🆕 온보딩 연출(전면 재설계, 2026-06-19 — '탭으로 깨우기')** — 원칙 **"손맛 먼저, 서사는 손맛 위에 얹는다"**(외부 멘토 피드백). 옛 모달 3컷(아래)을 폐기하고 **길 위에서 탭으로 깨우는 연출**로 교체. ① **누운 로봇**(고철 더미에 쓰러진 회색 로봇, -80°) + "눌러서 깨워주세요" → ② **탭마다 발(바닥)을 축으로 일어선다**(누움 -80°→기립 0°, 점증 + 스프링 베지어로 통! 하고 섬, `#walkRobot.waking`; 깨어날수록 grayscale 해제=생명). 6탭(`WAKE_TAPS`)에 완전 기립. ③ **결핍 박기**: 깨어나도 **가슴 코어만 빈 구멍**(`#app.coreEmpty`, `learned===0`일 때). 기립 순간 손맛 멈추고 **"…여기만, 비어 있다"** → **`[ERROR: 창조주 동기화 실패]`**. **첫 감정을 줍는 순간부터 기존 `--p`(마음 온도)가 코어를 채움** → 엔딩(인간化·가슴 하트)에서 완성 = 오프닝 빈 소켓의 페이오프(thru-line). ④ → **샘 등장**(사람 인라인 SVG, 샘 알트먼 × 일론 머스크 믹스 — 그레이 쿼터집 풀오버·비대칭 스마크)으로 길에 나타나 말풍선으로 주고받기(로봇 "나 사람 아니야?" → 샘 "응. 넌 로봇이야" → 로봇 "그럼 사람으로 만들어줘!" → 샘이 퀘스트 부여: 마음27·몸11 모아 와). ⑤ **샘 퇴장 직후 이름 짓기**(`SAM_SCRIPT` `{naming}` 씬스텝 — *사명 받은 뒤 짓는 이름 = 각오*. 옛 '모달에서 먼저 이름'에서 이동). ⑥ 이름 뒤 **로봇이 규칙 설명**(걸으면 만남=마음·몸 한 조각 / 탭=가속) → 걷기 시작. **걷기 시작 시 `INTRO_SYSLOG` 고정 3줄 우선 소비**로 오독개그 "(…새다)"를 1분 안에 터뜨림(첫 미소=차별점·바이럴 씨앗). 구현: `addWalk`에 `intro.phase`(wake/wakedone/play) 가로채기 솔기 + idle 루프 wake 중 정지, `startWake`/`renderWakeFrame`/`wakeTap`/`finishWake`/`openNamingModal`/`SAM_SCRIPT[]`/`startSamScene`/`renderSceneBubble`. ⚠️ 옛 `INTRO` 모달 3컷·`renderIntro`/`finishIntro`/`introSkip`은 제거(깨우기 연출로 흡수). 브라우저 풀체인 검증 완료.
> - **🆕 걷기 화면 「오류 로그」(syslog, 구현 완료)** — 걷는 동안 화면에 로봇의 상태 로그가 흐른다. **로봇은 감정을 '오류·고장'으로 읽는다**(§4 무기 = 순진한 재정의): 진행도(배운 감정 수)에 따라 차가운 기계 로그(`[시스템] 좌측 모터 효율 0.81`) → 감정이 새어나오는 로그(`[오류] 시각 센서에 액체. 배출 중…`) → 배운 감정 인용(`[재분류] 오류 → 감정 '슬픔'. 고장 아님.`)으로 따뜻해짐. 멍한 idle 시간을 서사로 채움 + LLM 풀 보충 훅 `addSysLogs()` 마련(§7).
> - **톤**: 거지키우기처럼 가볍고 귀엽되 뭉클. 로봇의 순진한 일기 = 무기. 스토리 폰트 = Gaegu(개구체).
> - 🚫 §4 금기 유지: 다음 감정/이야기를 광고·결제 뒤에 가두지 말 것.

### 통화 + 지도 (확정 — 단일 「걸음」 + cells식 그래프)

> **통화는 「걸음」 하나.** 골드처럼 추상적이지 않고 **여행 그 자체** — "한 걸음씩 사람에게 다가간다"가 곧 서사라서, *걸음 오름 = 길이 열림 = 이야기가 가까워짐.* §4 해자가 통화 이름만으로 지켜진다. (§6 원래 추천 "걸음거리 게이팅"을 통화로 승격시킨 형태.)
>
> - **🦶 걸음** (단일 통화) — 걷는 동안 **1초 1걸음** 자동 적립 + **탭으로 가속**(cells식 손맛). 소비처 = ① 지도 이동(간선 통과) ② 감각 업그레이드(생성기).
> - **🌊 「마음의 깊이」** (프레스티지 메타통화) — 회차 졸업 시 영구 획득.
>
> **지도 = cells식 마구잡이 유기적 그래프.** 직선 길이 아니라 **노드(사람·업그레이드·분기점)** 가 **간선(걸음으로 통과)** 으로 얽힌 그래프. 갈래가 뻗고, 이미 만난 사람에게 **되돌아가 더 깊은 대사를 듣는 재회**도 가능(프레스티지의 "같은 인물 더 깊은 대사"와 맞물림). **다음 노드는 안개 속 흐릿한 빈 원**으로 예고(cells, §3-4 호기심 연료).
>
> 흐름: `걷기/탭 → 걸음 적립 → 적립분을 연료로 그래프 위 원하는 노드로 이동 → 사람 노드 도착 = 만남 → 감정 해금(4중 보상) → 해금이 걸음 산출 ↑`.

### 5-A. 지도 구체 사양 — **cells 초기버전을 거의 그대로** (확정)

> 방향 확정: **Cell to Singularity 초기버전(2D 진화 테크트리 + 엔트로피 탭)의 지도 구조를 거의 그대로 차용**한다. cells의 검증된 뼈대를 가져오고, 통화·테마만 우리 것(걸음·감정)으로 치환한다.

**(1) cells 초기버전 구조 (우리가 베끼는 원본)**
- 중앙의 **세포를 탭**해서 1차 통화 **엔트로피(∆)** 를 생산.
- **세로로 뻗는 노드 테크트리**: 노드 = 진화 단계(세균→다세포→어류→양서류→파충류→포유류→영장류→인류→문명→AI→특이점).
- 각 노드는 **엔트로피로 언락**. 언락하면 **연결된 다음 노드(들)가 드러난다.** 아직 못 산 노드는 **흐릿한 외곽선 빈 원**으로 미리 보임.
- 일부 노드는 **생성기**(자동으로 엔트로피 생산, 지수 비용으로 업그레이드).
- 트리가 **갈라진다**(분기). 위로 스크롤하며 시대(era)가 바뀌고 화면이 변한다.
- 끝에 **프레스티지(특이점 리부트)** → 메타통화로 영구 배율.

**(2) 1:1 매핑 (cells → AI인간)**

| cells 초기버전 | AI인간 |
|---|---|
| 세포 탭 → 엔트로피(∆) | 화면 탭 → **걸음** |
| 진화 테크트리 노드 | **마구잡이 그래프 노드** (사람·감각·분기점) |
| 노드 언락(엔트로피 소비) | **간선 통과(걸음 소비)로 노드 도착** |
| 생성기 노드(자동 엔트로피) | **감각 업그레이드 노드**(자동 걸음/초) |
| 흐릿한 외곽선 빈 원 | **안개 속 빈 원**(다음 노드 예고) |
| 시대 전환·화면 변화 | **수채 색의 번짐**(회색→유채색) |
| 특이점 리부트 / 메타통화 | **회차 여행 / 「마음의 깊이」** |

**(3) 각 노드는 '끝'이 있다 + 재회로 다시 깊어진다 (cells의 생성기 업그레이드 그대로)**
cells에서 이미 언락한 생성기(세포·세균…)로 **되돌아가 반복 업그레이드**(레벨업·지수 비용·산출↑)했듯 — 우리 노드도 똑같다. **첫 도착은 완결(끝)되지만, 그 노드는 다시 찾아가 더 깊어지는 상대가 된다.**
- **첫 도착** → (사람 노드면) AI 사연 1편 → **감정 해금** → 그 만남은 **완결(끝)** 표시. 시작–본문–**끝**이 분명(무한히 안 늘어짐).
- 완료 노드는 **영구 보존** + **연결된 다음 빈 원들을 펼침.**
- **🔁 재회(되돌아가 업그레이드) = 핵심 손맛.** 같은 사람에게 **다시 가 걸음을 써서 더 깊이 이해**하면 **레벨업**: ① **걸음/초 ↑**(생성기 강화) + ② 그 사람의 **더 깊은 대사·편지 한 조각**(서사)을 **동시에** 해금. → *grind(숫자 업)가 곧 관계가 깊어지는 서사.* (cells 생성기 업그레이드를 감정으로 치환 = §4 해자 강화.)
- 재회·심화는 **같은 회차 안에서도** 가능(cells처럼 아무 때나 되돌아가 업그레이드). **프레스티지 회차**는 거기서 **더 깊은 상위 티어 대사**를 추가로 연다 — 같은 인물, 더 깊은 층.

**(4) 노드 종류**
- **👤 사람 노드** — 만남이자 **업그레이드 가능한 생성기.** 첫 만남(끝) 후 **재회로 반복 레벨업**(걸음/초 ↑ + 더 깊은 대사·편지). 그래프의 서사 심장 = 주력 생성기.
- **🛠 감각 노드(보조 생성기)** — 비인물 환경 생성기(빗소리·바람·햇빛…). 재방문 반복 업그레이드로 자동 걸음/초 ↑. 지수 비용.
- **🔀 분기점 노드** — 갈래가 갈리는 교차점. 어느 방향으로 갈지 선택(마구잡이 그래프의 손맛).
- **🌫️ 빈 원(미언락)** — 흐릿한 예고. 걸음으로 도달하면 정체가 드러남.

**(5) 그래프/언락 규칙**
- 노드 간 **간선마다 걸음 비용**(거리). 멀수록·깊을수록 지수적으로 비싸짐 = 코어 게이팅.
- 인접 노드만 보임(빈 원), 더 먼 건 안개. **언락이 지도를 키운다**(cells식 확장 = 성장 체감 §3-6).
- **마구잡이 유기 그래프**: 깔끔한 트리가 아니라 갈래가 얽히고 되돌아가는 형태. 그래도 각 노드는 finite.
- **🆕 갈래 안 대/중/소 단계 공개(2026-06-17)**: 각 갈래(4대분류) 안에서 대표 감정 **대** 1개만 먼저 열리고, 그 갈래에서 **재회 1회 → 중(다음 3-4개), 재회 2회 → 소(깊은 나머지)** 가 안갯속에서 드러난다(`TIER`·`regionReunions`). *재회(grind)가 곧 더 깊은 감정을 여는 열쇠* = §4 해자 강화. ⚠️ **(2026-06-18 v1.0.11~12 변경)** 4대분류는 이제 **순차 게이트**(이전 갈래 '완성' 시 다음 해금, `CAT_ORDER`), **몸(11)은 감정27 다 모은 뒤 맨 마지막**. 옛 '대분류 자유 진입·감정↔몸 병렬'은 폐기.

**(6) 시각/성장** — 위로(또는 바깥으로) 스크롤하며 **그래프가 펼쳐지고, 도달한 영역마다 수채 색이 번진다.** cells의 "시대 전환 = 화면 변화"를 **"감정 해금 = 색의 번짐"** 으로 치환.

**🚶 걷기 (idle 척추)** — 로봇이 끝없는 길을 자동으로 걷는다. 걷는 동안 **걸음**이 자동 적립(1초 1걸음, idle 수입). **화면을 탭하면 더 빨리 걷는다**(cells식 능동 가속, 손맛). **탭 = 핵심 가속**(탭당 걸음 추가), 입력은 단순 탭 하나로 통일 — **꾹 누르기/홀드 기능은 없음.** 적립된 걸음은 그래프 위를 이동하는 연료가 된다.

**👤 만남 (active + 언락 리듬)** — 그래프의 **사람 노드**에 도착하면 사람을 만난다. **다음 노드는 안개 속 빈 원으로 항상 흐릿하게 예고**(cells의 빈 원). 적립한 **걸음**으로 간선을 통과해 노드 **도착 → 사연(AI 생성) → 감정 해금**. (이동 거리 자체가 게이팅 = "이해하려면 다가가야 한다.")

**감정 해금 1회 = 4중 보상 (number-go-up의 감성판):**

| 보상 | 효과 |
|---|---|
| 📈 영구 성장 | 걸음 적립 속도 ↑ (더 깊이 느낄수록 빨리 걷는다 = 지수 엔진) |
| 🎨 색의 번짐 | 회색 세계에 새 색이 수채로 번짐 (성장=변화) |
| 📖 로봇의 일기 | AI 생성 한 편 (배우는 보상 + 공유 떡밥) |
| 🌫️ 다음 예고 | 다음 감정 노드가 흐릿하게 등장 (호기심) |

**🛠 생성기 (재회 + 감각 업그레이드)** — 걸음 산출을 키우는 두 갈래. ① **재회(주력)**: 이미 만난 **사람 노드로 되돌아가 걸음을 써 레벨업** → 걸음/초 ↑ + 더 깊은 대사·편지(§5-A 3). ② **감각(보조)**: "빗소리를 듣는다 +초당", "사람의 눈을 본다 ×1.5", "기억한다(자동으로 느끼기=자동화)". 둘 다 **지수 비용 vs 산출 = 코어 루프**(cells 생성기 업그레이드 그대로).

**🔁 회차 여행 (프레스티지 — 게임의 심장)** — 길 끝에서 로봇이 *"난 아직 사람이 아니야"* 를 깨닫고 **스스로 다시 걷기로 결심**(리셋을 캐릭터의 선택으로 정당화). 걸음·업그레이드·걸어온 그래프는 초기화되지만, 이번 회차에 배운 감정만큼 **「마음의 깊이」**(메타 통화)를 영구 획득 → 다음 회차 전체 가속 + **이전엔 못 본 같은 인물의 더 깊은 대사·편지가 열림.** → "다시 걷기가 매번 새 발견" → 중반 사막을 서사로 메움.

**🌙 오프라인** — 안 켜도 **걸음이 은행처럼 적립**된다(캡 있음). 복귀 시 *"당신이 없는 동안 로봇이 걸은 길"* 을 짧은 엽서/일기로 보여주고 → **적립된 걸음을 연료 삼아 그래프 위 원하는 노드로 이동**(돌아올 때마다 "어디로 갈지" 선택권 = cells식 복귀 손맛, §3-5 손실회피 재접속).

**💰 수익화 (서사 비파괴형만)** — 광고: ×2 가속·오프라인 2배·"이 만남 한 번 더 천천히 보기". IAP: 광고 제거·영구 ×2·수채 팔레트 스킨. **첫 경험은 광고 0.** 스토리는 절대 안 가둠.

**🕊 엔딩** — Spaceplan식 *"마침내 사람이 되었다"* 클라이맥스를 분명히 두되, 그 후 "더 깊은 회차"를 선택지로(무한 grind보다 강한 정서 클로징 + 원하면 계속).

---

## 6. 결정 완료 ✅ (모두 확정 — §5 반영됨)

1. **통화 구조 + 지도** → 단일 「걸음」 통화 + **cells 초기버전을 거의 그대로 차용한 마구잡이 그래프 지도**(노드·간선, 각 노드는 '끝'이 있는 완결 유닛). 상세 사양 = **§5-A**. 오프라인엔 걸음 적립, 복귀 시 그래프 이동. 메타 = 「마음의 깊이」.
2. **탭 비중** → **탭 = 핵심 가속 확정.** 한 번 탭당 걸음 가속(cells식 손맛). **꾹 누르기(롱프레스/홀드)는 별도 기능 만들지 않음** — 입력은 단순 탭 하나로 통일.
3. **AI 생성 범위** → **미리 생성 캐싱 풀 + 가끔 신선 생성** 확정. 일기·사연을 풀에서 즉시 서빙(오프라인·저비용·즉응), 가끔만 신선 LLM 호출로 풀 보충 → 비용·콜드스타트 방어.

→ 핵심 설계 확정 완료. 이제 MVP 코어 루프 구현 단계.

---

## 7. 기술 메모

- 거의 **클라이언트 단독** + Supabase(세이브/랭킹). Render 무료티어 슬립다운 영향 적음(PWA 오프라인 플레이).
- **⚠️ 세이브 정책(의도된 설계 — 함부로 바꾸지 말 것)**: **둘러보기(로그인 X)는 진행을 어디에도 저장하지 않는다 — localStorage조차 안 씀.** 익명 플레이는 **휘발**(새로고침=초기화)이며, 이는 **로그인을 유도**하기 위한 결정. `saveState()`의 `if(!authUser) return;`이 그 핵심(없애면 안 됨). 로그인 유저만 localStorage 미러(빠른 부팅) + Supabase 동기화. **세션 유지(`createClient persistSession:true`)로 자동 로그인은 그대로** — 단, **로그아웃 후 다시 로그인할 때만** 구글 계정 선택창이 뜬다(`signInWithOAuth prompt:select_account`). 로그인 버튼은 로그아웃 상태에서만 눌리므로 **평소 자동로그인엔 영향 없고**, "다른 계정으로 들어가고 싶을 때 로그아웃→재로그인"만 계정 선택 가능(2026-06-17). ⚠️ 따라서 **둘러보기 유저가 새로고침 후 다시 온보딩을 보는 건 정상**(세이브가 없으니까). 단, **진행 중(인메모리 진행도가 있는 동안)엔 온보딩이 다시 뜨면 안 됨** — `hasProgress()` 가드(`startIntro`·`startSamScene`)와, 클라우드 세이브가 비동기로 늦게 도착하면 진행 중이던 온보딩을 걷어내는 `dismissOnboarding()`(`onAuthChanged`)로 방어. (2026-06-17 버그픽스: 로그인 복귀 유저에게 온보딩이 잠깐 뜨던 레이스 차단.)
  - ✅ **문서 일치(2026-06-17 완료)**: `privacy.html`·`delete-account.html` 문구를 실제 설계(둘러보기=비저장, 저장·동기화는 로그인 시에만)에 맞게 수정함.
- 로봇의 일기·만남 사연은 **Claude로 생성하되 미리 생성 캐싱 풀에서 즉시 서빙**(오프라인·즉응·저비용). 풀이 줄면 **가끔만 신선 LLM 호출로 보충** → 콜드스타트·비용 방어. LLM은 콘텐츠 무한 공급 = 중반 사막 방어 무기.
- netcode 없음 → 실시간 멀티 기술 리스크 자체가 없음(저위험).
- **로컬 실행**: `npm install`(최초 1회) → `npm start` = `live-server` 핫리로드(`localhost:8000`). 빌드·번들러 없음(단일 정적 `index.html`).

## 8. 경로 / 상태

| 대상 | 경로 |
|---|---|
| 리포 루트 | `/home/jammy/projects/ingan` (로컬 폴더명은 `ingan`, 원격 리포명은 `ai-ingan`) (git, 원격: `https://github.com/jammy0903/ai-ingan.git`, branch `main`) |
| 게임 본체 | **분리됨(2026-06-22)**: `index.html`(HTML+`<script src>` 나열) · `styles.css` · `data.js` · `balance.js` · `engine.js`(상태·산출/탭·도달판정) · `view.js`(렌더·모달·이동만남·탭·하늘·syslog) · `save-auth.js`(세이브·로그인·어드민) · `onboarding.js` · `main.js`(부팅). **전역 클래식 스크립트(모듈/빌드 없음), 로드 순서=의존 순서.** sw.js 네트워크우선 정규식·CORE에 전부 등재 |
| 확정 아트톤 목업 | `mockup/tone-c-watercolor.html` |
| 참고(탈락) 목업 | `mockup/tone-a-pixar.html` |

- **현 상태**: MVP 코어 루프 + **도파민/경제 개편(2026-06-18)** 동작. 걷기+탭(걸음/초 비례+콤보×7+크리) · cells식 그래프(**대분류 순차·몸 마지막**) · 만남/재회(끝없는 sink, 5레벨마다 ×2 달성보너스) · **마일스톤 배율로 income 지수 폭발(K/M/B 표기)** · **온보딩 '탭으로 깨우기'(누움→기립 회전·빈 코어·이름 뒤로, 2026-06-19)** · 세이브/구글로그인 · 엔딩 시네마틱 · PWA · **감정27 콘텐츠 5단+pick(2026-06-19)** · **재회 모달 '이전 대사' 버튼+재회 즉시반응(v1.0.18)** · **하늘 데코: 광고 비행기(펄럭 배너=추후 광고 슬롯)+표정 구름(터치=뿅·물비·걸음/초×10 선물, v1.0.19)**. **현재 배포 버전 v1.0.19**(라이브 aingan.click, Cloudflare 자동배포 — 연결·반영 검증됨). 핵심 설계 확정(§6). ⚠️ **2026-06-18 캐넌/경제 변경(v1.0.11~12, 아래 '최근 작업' 참고)**: 발견비용=income추종 탭곡선 · 대분류 순차 게이트 · **몸=감정27 뒤 맨 마지막(병렬→순차로 회귀)** · '그 사람'(창조주 연모) 서사 제거 · 전 유저 1회 리셋. 부족분 분석·로드맵 = `gap-analysis.md`.
- **로컬 실행**: 정적 파일이라 빌드 없음. `python3 -m http.server 8000` 후 `localhost:8000` (구글로그인/클라우드세이브까지). 핫리로드 원하면 `npx live-server`. 직접 파일 열기도 됨(단 OAuth는 막힐 수 있음).
- **⚠️ 핫리로드 없음** — 코드 고치면 브라우저 수동 새로고침(F5). 검증은 `node --check`로 인라인 스크립트 문법 → 브라우저로 동작 확인.
- **⚠️ 버전 = semver `1.0.N`(2026-06-18 도입). `data.js`·`balance.js`·자산 수정 시 버전을 반드시 올리고 3곳 동기화: `sw.js` `CACHE="aingan-1.0.N"` · `manifest.json` `"version"` · `index.html` `#appVer` 표시** — SW가 스크립트를 '캐시 우선'으로 서빙해서, 안 올리면 고쳐도 옛 캐시가 나간다(stale). 콘텐츠·그래프 데이터는 `data.js`, idle 튜닝 상수는 `balance.js`로 분리됨(전역 classic 스크립트, 인라인보다 먼저 로드; 모듈 X·`file://` 유지). 둘 다 `sw.js` `CORE`에 등재. 상세·근거 = `plan.md`.
- **🔥 최근 작업(2026-06-22 대규모 세션 — 경제 재설계 1단계 + 구조 분리 + 몸 상점, v1.0.20→1.0.38 라이브 배포)**: 한 세션에 12+ 커밋 main 직접 푸시(CF 자동, **현재 배포 = v1.0.38**). 위 '현 상태'의 옛 경제 설명(걸음/초 비례·마일스톤 폭발·K/M/B)은 아래로 **대체됨**. 핵심:
  - **경제 재설계 1단계(`economy-redesign.md`, 커밋 f8593a5)**: idle 이중지수 폐기 → **걷기=1걸음/초 고정**(`effRate()=1`), 성장은 **탭 파워=`1+감정수+재회레벨합`**(`tapPower()`, 감정/재회마다 +1). **마일스톤 제거**(rateMult 미사용). **`reachCost=node.w` 평탄**(effTap추종·W_MULT 폐기). 오프라인=1걸음/초·상한 1만(`OFFLINE_CAP_STEPS`). 재회/몸 모달 '초/걸음'→'⚡탭+1'. 옛 idle곡선 함수(baseRate/nodeSps/fmtSps/MILESTONES 등)는 정의만 보존(디버그·세이브 호환).
  - **걸음 풀숫자 표기(커밋 a953529)**: `fmt()` K/M/B 폐기 → 콤마 풀숫자(1,500,000), 상한=`MAX_STEPS`(Number.MAX_SAFE_INTEGER), `syncSteps` 클램프.
  - **온보딩 버그픽스(커밋 906895c)**: 게이트 떠있는 동안 idle이 돌아 노드 자동발견→`hasProgress` 오판으로 온보딩 스킵되던 것. `showGate()`가 `intro.phase="gate"`로 게임 정지 + `hasProgress()`를 '실제 만남(레벨>0)+의도플래그'로 재정의(안갯속 발견·idle걸음 제외).
  - **🏗 구조 분리(커밋 f3282f2·e9fe068)**: 인라인 단일파일(2791줄) → **CSS=`styles.css` + JS 5분할(engine/view/save-auth/onboarding/main)**. 전역 클래식 스크립트(모듈/빌드 없음, file:// 유지), 로드 순서=원본 순서라 동작 보존. index.html=486줄. (§8 경로표·아래 리팩토링 메모 갱신됨)
  - **관리자 유저목록 유저명 표시(커밋 15b3a71·d147cf9)**: 행·확인창·상태메시지 전부 uuid→로봇이름(`_admNames` 맵, uuid는 호버 title).
  - **경제 튜닝 패널 정리(커밋 4747daa)**: 재설계로 죽은 손잡이 4개(재회초/걸음·마일스톤·발견곡선) 제거 → 살아있는 2개만: **🦶발견 비용 ×배율**(`DISCOVER_MULT` engine.js, 기본1) · **🔁재회 첫 비용**(UP_BASE.person).
  - **몸 온기 상점(커밋 93806e7·e94fe3c)**: 몸 11을 **그래프에서 제거**(NODES엔 유지=배치보존, 렌더/발견만 스킵) → **자루(기억 약장)에서 ✨온기로 구매/단련**. 첫구매(balance.js `BODY_BUY` 합25 ≤ 감정27코인 → §4 무광고 인간 달성 보장)→첫이야기 / 이후 '단련'(`BODY_TRAIN_BASE/GROW` 점증)→깊은 이야기 2~5단+탭+1(큰 소비처). 감정27 뒤 열림. 엔딩=감정27+몸11'구매'(bodyCount 그대로). `bodyShopHtml/buyBody/bodyStoryModal`(view.js).
  - ⚠️ **펜딩/미검증**: ① **몸 상점 B2 브라우저 풀검증 미완**(로컬서버가 빈 응답 주는 글리치로 막힘 — node --check는 전부 통과·B1 그래프숨김은 검증됨). **배포본에서 감정27 도달 상태로 몸상점 스모크테스트 필요.** ② **경제 재설계 Stage 3 미적용**: `data.js`의 w값이 아직 옛날 값(joy=40) → "대분류당 하루" 스케일(`economy-redesign.md` §2 4대분류 표)을 data.js에 박아야 함(data.js 수정=버전범프). 현재는 진행이 빠름. ③ **Stage 2**(콤보/크리 완만화) 미착수.
- **최근 작업(2026-06-19 후속 — 재회 모달 '이전 대사' + 발견비용 w 시스템 + 하늘 데코, 커밋 045ce8b·e82231d·4e15a91)**: main 직접 배포(CF 자동, 라이브 1.0.19 검증). 핵심:
  - **발견비용 income추종 'w' 시스템(v1.0.11~12 설계의 데이터화, 커밋 045ce8b)**: `data.js` 각 노드에 `w`(발견 목표 걸음) 부여 + `balance.js` `DISCOVER_BASE`. 실제 비용 = `w × max(1, effTap/DISCOVER_BASE)`(`reachCost`) — 초반엔 `w` 그대로 노출("재미=250걸음"), income 폭발 후반엔 추종해 커짐. `TAP_CURVE`는 `w` 없는 노드용 폴백으로 격하.
  - **재회 모달 '이전 대사' 버튼(v1.0.18, 커밋 e82231d)**: 모달의 '다시 만나 더 깊이'(`#mBtn2`)를 절반 폭 `🔁 재회`(몸=`🔨 단련`)로 줄이고, 빈 자리에 `📖 이전 대사`(`#mPrev`/`#mActions` flex 반반) 추가 — 지금까지 본 대사를 구분선으로 이어 다시 보기(읽기 전용, `next` 콜백으로 직전 재회 모달 매끄럽게 복귀; `showPrevLines`). 최대 레벨이면 이전 대사만 노출. **재회 즉시반응**: `upgrade`가 이미 노드 위인데도 걸리던 `travelTo` 1.1초 대기 제거.
  - **하늘 데코(v1.0.19, 커밋 4e15a91)** — 걷기 화면 하늘에 떠다니는 장식 + 손맛 보상. ⓐ **광고 비행기**(`#planes`/`spawnPlane`): 펄럭이는 배너(`bannerwave`)를 끌고 상단을 가로지름. 배너 문구는 **`planeBannerText()` 풀**(지금은 귀여운 문구, **나중에 여기를 광고 소스로 교체**하면 그대로 광고 — XSS 대비 `esc()`). ⓑ **표정 구름**(`#clouds`/`spawnCloud`/`CLOUD_SVG`): 비행기보다 아래·약 2배 빠르게(dur 8.5~12s)·반대 방향, 2배 덜 등장(52s 인터벌). **터치하면 뿅 터지고(`cloudpop`) 물방울이 비처럼(`raindrop`/`rainBurst`) 쏟아짐 + `effRate()×10` 걸음 선물(`popCloud`→🎁 팝업·`syncSteps`/`refreshHUD`)**. 탭은 `#pageWalk`로 버블 → 걸음 가속에도 반영. ⓒ `#syslog` `top:72→108px`(감정 카운터·비행기 띠와 겹침 해소). ⚠️ 비행기/구름은 `z-index 3/2`로 HUD(4·5) **뒤로** 지나가 텍스트를 안 가림. ⓓ 선물 배율은 현재 **×10**(1걸음/초→10, 2.7→27) — 추후 ×18(2.7→~50) 상향 여지.
- **최근 작업(2026-06-19 — 온보딩 '탭으로 깨우기' 재설계 + Phase 2 콘텐츠/줍기, 커밋 3a785f8·9845d2a·b425ea9)**: 외부 멘토 피드백("손맛 먼저") 반영, plan.md §7 3스테이지로 분할 구현. **스테이지 1+2+3 완료**(브라우저 풀체인 검증). 스테이지 3은 검증 결과 **코드 변경 불필요** — 첫 감정(기쁨, `joy="big"`) 도달 ≈30걸음으로 3분 내 이미 충족(introDiscount 불필요), '따뜻한 성취'는 빈 코어→`--p` 채움 페이오프가 해결(어두운 구멍→teal 코어, 카지노 연출은 의도적 배제). 핵심:
  - **탭으로 깨우기** — 옛 모달 3컷 폐기. 길 위 누운 로봇(-80°)을 탭으로 일으켜 세움(발 축 회전 + 스프링 베지어, `#walkRobot.waking`/`renderWakeFrame`). 깨우기=탭 → "왜 탭하면 빨리 걷지?" 의미론적 배신 봉합.
  - **결핍=빈 코어** — `#app.coreEmpty`(learned===0)로 가슴 코어를 빈 구멍으로, 첫 감정부터 기존 `--p`가 채움(엔딩 하트=페이오프). blast radius: 기존 마음온도 피드백과 충돌 대신 통합(안 죽임).
  - **이름짓기 이동** — 모달 먼저 → 샘 퀘스트 직후(`SAM_SCRIPT {naming}` 씬스텝). **오독개그 선배치**(`INTRO_SYSLOG` 고정 3줄, "(…새다)" 둘째 줄).
  - 제거: `INTRO`/`renderIntro`/`finishIntro`/`introSkip`. `addWalk`에 `intro.phase`(wake/wakedone/play) 솔기 + idle 루프 wake 중 정지.
  - ⚠️ **온보딩 버전 무변(1.0.12 유지)** — 온보딩 변경이 `index.html` 인라인뿐(data.js/balance.js/자산 불변). navigate=네트워크우선이라 SW stale 없음 → 범프 불필요. 상세 = `plan.md §7`.
  - **Phase 2 감정27 콘텐츠 와이어링(v1.0.13, 커밋 9845d2a)**: `data.js` PEOPLE 27개에 `pick`(오독 개그)·`lines` 3→5단(관찰→자기→심화→더깊은자기→재정의 "그게 ○○이래")·자기-곱씹기 `mem` 부여, '그 사람'(창조주 연모) 흔적 0개로 재작성. **data.js 수정 → 버전 1.0.12→1.0.13 범프**(sw.js·manifest·#appVer 3곳 동기화).
  - **pick 줍기 연출(커밋 b425ea9)**: 감정 노드 **첫 도착** 시 모달 전에 글자 조각(📃) 떠오름 + 로봇이 `pick` 텍스트로 오독("‘기쁨? 거품?’") → 페이드 → 첫만남 모달이 정정. `pickFx(n,cb)` + `.pickFrag`/`.pickCap` CSS, `tryUnlock`의 travelTo 콜백(person·pick 보유 시만) 훅. index.html 인라인뿐 → **버전 무변(1.0.13 유지)**. → **Phase 2 완료**(잔여 저우선: 몸11 콘텐츠 5단).
- **최근 작업(2026-06-18 — v1.0.11~12 경제/온보딩 개편 + 배포)**: 한 세션에 다수 변경 → main 직접 배포(CF 자동, 라이브 1.0.12 검증). 핵심:
  - **발견(이동) 비용 = income추종 '탭 곡선'**: `reachCost = effTap × TAP_CURVE[순번]`(`[5·25·42·58·80]`, balance.js). 순수 탭수=곡선값 고정(초반 손맛 램프→평탄), 후반 공짜/벽 동시 방지. 옛 고정 간선비용(`node.cost×COST_MULT`)은 비용엔 미사용(EDGES는 토폴로지·렌더용으로만 유지).
  - **재회 곡선 완만화** `upCost` 1.22→1.15(국소 벽 제거) · **오프라인 적립 1/10**(`OFFLINE_RATE=0.1` — 온라인의 1/10 = 1분 6걸음)+복귀 인사("당신이 없는 동안 N걸음이나 걸었어요", `fmt`).
  - **대분류 순차 게이트**(`CAT_ORDER`·`categoryComplete`·`gateOk`): 긍정만 처음 열림 → 이전 갈래 '완성'(그 갈래 감정 전부 수집) 시 다음 해금(긍정→강한자극→불안·불편→잔잔·시림). **몸은 감정27 다 모은 뒤 맨 마지막**(`gateOk` body = `learnedCount≥27`). ⚠️ 옛 '감정↔몸 병렬·대분류 자유진입' 폐기.
  - **재회 UX**: 막다른 길 직전 1회 코치 안내(`#coach`/`maybeTeachReunion`/`S.seenReunionHint`, "재회해야 다음 열림") + 노드 위 **'다시 만나기' 버튼**(`#reunionBtn`/`isReunionable`/`updateReunionBtn` — 로봇이 노드 가림 해소) + 새 노드 발견 시 그곳으로 **줌인 카메라 이동**(`panTo`, REVEAL_ZOOM 1.1; 네이티브 smooth 막힌 환경 대비 rAF 트윈).
  - **'그 사람'(창조주 연모) 서사 전면 제거**: 감정 mem 23개를 로봇의 순진한 자기-곱씹기로 교체(사람 언급 X) + 보상라벨·syslog·data.js 로어 주석 정리. (`그리움`은 추상화)
  - **전 유저 1회 강제 리셋**(`SAVE_VERSION` 1→2 마이그레이션 — `v<2` 진행 폐기→처음부터) · **모바일 박스 분기 고침**(`@media (max-width:600px),(pointer:coarse)…`로 폰 풀블리드) · **온보딩 '튜토리얼 넘어가기' 버튼**(`#samSkip`→`endSamScene`) · **'다음 속도업까지' HUD(mindsky) ↔ syslog 겹침 수정**(syslog `top:50→72px`).
- **이전 작업(2026-06-18 초 — 도파민/경제 1차 + 목표문장 설계)**: 신규 문서 `gap-analysis.md`(cells·거지키우기 대비 부족분 분석 + 6단계 로드맵)·`content-emotions-draft.md`(감정27 Phase2 콘텐츠 초안, 몸 제외).
  - **Phase 1 탭 손맛**: `effRate()`/`effTap()` 계산 솔기 분리(refactor) → 탭=걸음/초 비례(`TAP_FRAC=1.0`, "1탭=1초어치", 죽은구간 제거) → 연타 콤보(`COMBO_WINDOW/STEP/MAX`, 최대 ×7)+크리(`CRIT_CHANCE 0.12·CRIT_MULT 6`, 변동보상). 콤보 뱃지·금빛 크리 팝업.
  - **마일스톤 배율**(`MILESTONES`, 점증 ×1.6→×15, 누적 ×약3.7만): (감정+몸) 수집 개수가 문턱[3·6·10·15·20·27·33·38] 넘을 때마다 전역 걸음/초 ×점프(거지 달성보너스식). `rateMult()`로 idle·탭·**오프라인** 일괄 반영. HUD `🎯 다음 속도업까지 N개`.
  - **걸음/초 base = `baseRate()`로 levels에서 계산**(누적 `S.rate` 폐기 — `S.rate` 필드는 세이브 호환만 보존). `nodeOut(n,lv)=gen×lv×achieveMult(lv)`.
  - **재회 = 끝없는 sink**(거지키우기 알바식): `achieveMult`=5레벨마다 ×2 달성보너스, `upCost` ×1.45→1.22(완만), `MAX_LV` 10→50. 모달에 `✨ 달성 보너스 ×2` 연출 + 다음 예고.
  - **비용 밴드 재배치**: 4대분류 걸음비용 오름차순(긍정<강한자극<불안불편<잔잔시림) = 목표문장 4구절 순서. cost만 변경(EDGES 파생, computeRadialLayout 레이아웃 불변).
  - **K/M/B 표기**: `fmt()` 재작성(걸음·걸음/초·탭, Dc 넘으면 지수) — 미국 출시 대비 영문 단위(한글 만/억 X).
  - **버전 도입 1.0.10** + **Cloudflare 자동배포 확인**(푸시→aingan.click 자동반영; 배포설정은 리포 아닌 CF 대시보드. 라이브=1.0.10 검증).
  - ⚠️ **확정 설계(미구현)**: **목표 문장 = A뒤집힘 × C4절** — 진행 중엔 「이렇게 좋아하고·원하고·무서워하고 그래도 걷는 내가 — _사람이 되게 해줘?_」(빈칸=희망회로 오답 깜빡, 반전 누설 방어), **샘 재회에서 마지막 조각으로 뒤집힘** 「…그래도 걷는 내가, **벌써 사람이잖아.**」. 4구절(좋아하고=긍정·흥미 / 원하고=강한자극 / 무서워하고=불안·불편 / 그래도 걷는=잔잔·시림)이 **4대분류 완성마다 슬롯 점등**. 도파민 결론: 문장은 *엔진이 아니라 방향타+결승선* — 잦은 도파민은 거지키우기식 '잼'(감정 **줍기**·**오독 개그**·**달성 슬롯**)과 income 폭발에서 나옴. 콘텐츠/연출 초안 = `content-emotions-draft.md`.
  - 🔧 리서치는 **crawl4ai** 사용(cells 위키·거지키우기 namu.wiki). firecrawl은 두 키 모두 크레딧 소진(402); MCP 키는 새 키로 교체됨(적용엔 세션 재시작 필요).
- **이전 작업(2026-06-17)**: ⓐ **온보딩 연출 전면 개편** — 모달은 깨어남 3컷만, 그 뒤 **길 위 애니메이션**(샘=사람 인라인 SVG[알트먼×머스크], 로봇↔샘 말풍선 주고받기 → 샘 퇴장 → 로봇이 사용자에게 규칙 설명 → 걷기 시작). 대화 중 걸음 정지, 거리 넓힘, 말풍선 클램프+꼬리. (상세 = §5 "온보딩 연출" 불릿) ⓑ **감정 만남 모달 개편**: 노드 도착 시 **자리 반짝임**(`flashNode`) → 첫 만남 **2페이지 모달**(장면 → "『○○』/ 을(를) 알게 됐다" 해금, 이름만 크게·조사 자동). ⓒ **감정 대사 27개 정리**(첫 만남 끝 "이게 ○○이다" 제거, 민망함/끌림/연민 수정). ⓓ **대분류 걸음 비용** (2026-06-18 **오름차순 밴드 재배치** — 긍정<강한자극<불안불편<잔잔시림, 슬픔이 가장 비싸 마지막에 열림; 목표문장 4구절 순서와 일치. 옛값 긍정10·불안30·강한80·잔잔50). ⓔ **줌 입력**: 마우스 휠=줌(ctrl 불필요), 모바일 두 손가락 핀치 줌. ⓕ **로봇 얼굴 귀엽게**(큰 동그란 눈·밝은 하늘색→금색·흰 반짝임·볼 홍조·미소). ⓖ **엔딩 시네마틱**(`#endcine`·`ENDING_SEQ`): 풀스크린 한 줄씩 페이드 → "마법 부리듯—"에서 정적(hold) → "넌 벌써 사람인데"에서 **사람이 된 인간 SVG 등장 + 배경 따뜻**(회화풍 `#paintSoft`, 가슴 하트·눈물) → 클라이맥스 "나는, 사람이다" → 프레스티지. 옛 `ENDING_TEXT`/`SAM_TWIST`/`HUMAN_TEXT` 데드코드 제거·통합. ⓗ **갈래 안 대/중/소 단계 공개**(`TIER`·`regionReunions`·`gateOk`): 각 갈래 대표 감정 **대**(기쁨·두려움·신남·슬픔)만 먼저 열리고, 그 갈래에서 **재회 1회→중, 2회→소** 공개(크기도 대>중>소). ⓘ **PWA 셋업**(`manifest.json`·`sw.js`·`icon-{192,512}.png`·`icon.svg`): 설치 가능 + 오프라인(내비=네트워크우선, 자산=캐시우선, Supabase 제외), 앱 아이콘=귀여운 로봇머리+하트. **앱 표시 제목 「강철의 인간술사」**(⚠️ '강철의 연금술사' 패러디 — 상표/플레이 IP 리스크 사용자 인지 후 강행; 프로젝트 코드네임·도메인은 aingan 유지). 구글 플레이는 PWABuilder/Bubblewrap(TWA)로 래핑 예정. 모두 브라우저 검증 완료.
- **이전 작업(2026-06-16)**: ⓐ 감정 지도를 **root→4대분류→감정 3계층 그래프**로 개편(`type:"category"` 허브 4개 + 감정 27개 재분류) + **연관/반대 시각 전용 엣지**(`REL_EDGES`) 추가 — 기존 감정 27개 일기 콘텐츠는 100% 보존. ⓑ 그 전: 걷기 syslog · 감정↔몸 병렬화 · 온보딩 재작성 · 엔딩 반전. 모두 구현·검증 완료.
- **🔧 유지보수 리팩토링(①~④ 완료, 2026-06-18 / 커밋 b0e6fa0·78a51f0·df7faea) → `plan.md`**: MVP 단일 파일 → 경량 분리. **데이터(`data.js`)·밸런스(`balance.js`)만 전역 `<script src>`로 추출**(ES 모듈 X·폴더 트리 X·그래프 자동생성 X — 이 코드베이스 함정 3개 회피: SW 캐시우선 stale / `file://`·`node --check` 약화 / `computeRadialLayout` id-의존 배치 셔플). 순서 = ① 세이브 버저닝(`snapshot`/`applyState`) → ② `data.js` → ③ `balance.js` → ④ SW `CORE` 추가 & **데이터 수정 시 `CACHE="aingan-vN"` 범프 규칙**. 엔진·뷰는 인라인 유지. KV 핫업데이트·세이브 스로틀·Vite는 *기능 임박 시*로 연기. 상세·근거 = `plan.md`.
- **⚠️ 경제 모델 재설계(확정·미구현, 2026-06-22 숫자까지 확정) → `economy-redesign.md`**: idle 성장 폭발(이중 지수)을 폐기. **걷기(idle)=끝까지 고정 1걸음/초**, 성장(number-go-up)은 **탭 파워**로 이동 — **탭 파워 = 1 + 감정수 + 재회레벨합**(감정 1줍기 = 탭 +1, 재회 1레벨 = 탭 +1). **마일스톤(`MILESTONES`/`rateMult`) 제거**(자동 1 고정이라 곱할 데 없음). 발견비용 `reachCost=w`(effTap 추종·`W_MULT` 제거) → **`w`=그 노드까지 걷는 초**(화면에 그대로). 콤보·크리는 변동 양념으로 유지(상한은 2단계 미세조정). **오프라인=1걸음/초·누적 상한 10,000걸음**. **스케일 = 대분류 하나당 +1일**(가만히 두면 강한자극 도착 1일/불안불편 2일/잔잔시림 3일; 각 대분류 Σw≈86,400=하루). **목표 A: 빡탭 1~2시간 / idle 하루.** 몸11은 그래프에서 빼고 「온기 조각」 구매(Stage 2 대기). 3단계: ①엔진전환(`effRate`=1·`tapPower` 분리·`MILESTONES` 삭제·오프라인 단순화, 블래스트=idle루프·HUD·탭·오프라인적립·구름선물) ②콤보/크리 완만화 ③`data.js` w 스케일(4대분류 표 = 문서 §2). 상세·w표·검산 = `economy-redesign.md`. (이 방향이 §5 통화·§6 결정·아래 v1.0.34 `W_MULT=5`를 대체.)
- **참고 — v1.0.34 임시 밸런스 완화(커밋 19f4b07, 2026-06-22)**: `W_MULT=5` 신설(발견비용 ×5)·`MILESTONES` 후반↓(누적 ×3.7만→×7.9천)·`REUNION_FACTOR` 0.85→0.88. **위 경제 재설계로 대체될 임시값** — 재설계 1단계에서 `W_MULT=1`·idle 고정으로 갈아엎음.
- **다음 할 일(미완, 우선순위)**: ✅ **Phase 2 — 완료(2026-06-19)**: 데이터 와이어링(v1.0.13, 커밋 9845d2a: 감정27 `pick`+`lines`5단+'그 사람' 재작성) + **pick 줍기 연출(커밋 b425ea9)** — 첫 도착 시 글자 조각(📃) 떠오름+오독 말풍선(`pickFx`) → 모달이 정정. 잔여(저우선): 몸11(BODY) 콘텐츠 5단 확장은 미착수. ① **(다음 착수) 목표 문장 엔진 훅**(미구현 설계) — 감정 줍기 연출·4구절 슬롯 점등·빈칸 문장 HUD(희망회로 오답 깜빡)·샘 재회 뒤집힘(`ENDING_SEQ` 연동). ② **Phase 4 프레스티지 상점** — `depth`(마음의 깊이) 소비처(영구배율 구매); `MAX_LV=50`으로 depth가 크게 쌓여 시급(현재 소비처는 lookback뿐). ③ **경제 튜닝** — income 폭발 강도(`MILESTONES`·`achieveMult` 간격·`upCost`) 실제 플레이로 검증·조정. ④ Phase 5 LLM 풀·오프라인 엽서. 상세 로드맵·근거 = `gap-analysis.md`.
