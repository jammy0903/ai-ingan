# 감정 그래프 재설계 계획 (Emotion Graph Redesign)

> **상태: 구현 완료(2026-06-16).** 추천 디폴트로 착수하되 — **로스터는 기존 C&K27을 유지·재분류**(콘텐츠 전면 재작성 대신, 일기 100% 보존)했다. 슬픔 유지·"전부 수집=엔딩"·SFW '끌림'·연관/반대 시각 전용은 이미 코드에 있던/그대로 반영.
> 기존(`start → 6계열 → 감정`, 트리만)을 → **root → 4 대분류 → 세부 감정 + 연관/반대 교차 엣지가 있는 진짜 그래프**로 교체했다.
> 근거: UC 버클리(Cowen & Keltner, 2017) 감정 스펙트럼을 4영역 계층 + 관계 엣지로 구조화.
>
> ⚠️ **실제 구현된 4분류(코드 기준, 문서 §2 가안과 다름)**: 긍정·흥미(10) / 불안·불편(7) / 강한 자극(5) / 잔잔·시림(5) = 27. 분류 허브는 `type:"category"`, 관계선은 `REL_EDGES`(assoc/oppo). 상세 = `CLAUDE.md §5`.

---

## 1. 그래프 구조 (Nodes & Edges)

### 노드(Nodes)
- **Root**: `[감정 (Emotion)]` — 모든 것의 중심 (= 현재 `start`).
- **1차 자식 (대분류 줄기, 4개)**: `[긍정/흥미]`, `[불안/불편]`, `[강한 자극]`, `[잔잔함/기타]`
- **2차 자식 (Leaf, 세부 감정)**: 아래 27(또는 26)가지.

### 엣지(Edges) 연결 규칙
- **계층 연결(Parent-Child)**: 중심 → 대분류 → 세부 감정. (게임 내 걸음 이동 경로)
- **연관 연결(Association)**: 결이 비슷하거나 자주 함께 일어나는 감정끼리 가로 실선. (예: 불안 ── 두려움)
- **반대 연결(Opposition)**: 거울처럼 반대되는 감정끼리 점선. (예: 즐거움 ┄ 괴로움)

---

## 2. 27가지 감정 그래프 맵 (전체 구조도)

```
                              [감정 (Emotion)]
                                     │
      ┌──────────────┬───────────────┼───────────────┬──────────────┐
      ▼              ▼               ▼               ▼
 [긍정/흥미]     [불안/불편]      [강한 자극]      [잔잔함/기타]
      │              │               │               │
  ├ 감탄(Admiration)     ├ 불안(Anxiety)      ├ 분노(Anger)       ├ 차분함(Calmness)
  ├ 감상(Appreciation)   ├ 두려움(Fear)       ├ 흥분(Excitement)  ├ 향수/그리움(Nostalgia)
  ├ 즐거움(Amusement)    ├ 공포(Horror)       ├ 놀람(Surprise)    ├ 아련함(Melancholy)
  ├ 만족(Satisfaction)   ├ 어색함(Awkwardness) └ 성적 욕구(Desire)
  ├ 기쁨(Joy)            ├ 지루함(Boredom)
  ├ 열정(Interest)       ├ 혼란(Confusion)
  ├ 갈망(Craving)        ├ 혐오(Disgust)
  ├ 경외감(Awe)          ├ 부러움(Envy)
  ├ 로맨스(Romance)      └ 괴로움(Distress)
  └ 동정심(Sympathy)
```

| 대분류 | 세부 감정 | 개수 |
|---|---|---|
| 긍정/흥미 | 감탄·감상·즐거움·만족·기쁨·열정·갈망·경외감·로맨스·동정심 | 10 |
| 불안/불편 | 불안·두려움·공포·어색함·지루함·혼란·혐오·부러움·괴로움 | 9 |
| 강한 자극 | 분노·흥분·놀람·성적 욕구 | 4 |
| 잔잔함/기타 | 차분함·향수(그리움)·아련함 | 3 |
| **합계** | | **26** ← ⚠️ 27 목표 대비 1개 부족 (아래 결정 1) |

---

## 3. 엣지로 '감정의 그라데이션' 연결

### ① 강도(Intensity) 연관 엣지 — 유사하지만 에너지가 커지는 축
- 불안(Anxiety) ──▶ 두려움(Fear) ──▶ 공포(Horror)
- 만족(Satisfaction) ──▶ 기쁨(Joy) ──▶ 흥분(Excitement)
- 지루함(Boredom) ──▶ 혼란(Confusion) ──▶ 괴로움(Distress)

### ② 양가/이웃 엣지 — 경계에 걸친 복합 감정
- 로맨스(Romance) ── 부러움/질투(Envy)  *(동전의 양면)*
- 향수/그리움(Nostalgia) ── 아련함(Melancholy)  *(과거의 슬픔)*
- 경외감(Awe) ── 두려움(Fear)  *(압도당함)*

### ③ 반대(Opposition) 엣지 — 거울 쌍 (점선)
- 기쁨(Joy) ┄┄ 괴로움(Distress)
- 차분함(Calmness) ┄┄ 분노(Anger) / 흥분(Excitement)

---

## 4. 구현 계획 (코드 레벨)

1. **데이터 모델 교체**
   - 새 노드 타입 `category`(대분류 4) 추가 → `start(감정) ─ category ─ person(leaf)` 3계층.
   - `PEOPLE` 전면 개편: 바뀐/새 감정의 이름·색·`situ`·`lines[3]`·`mem`(로봇 일기) 재작성.
   - `NODES` / `EDGES` 재구성.
2. **엣지 타입 도입**
   - `EDGES`에 `type`: `hier`(계층, 이동 가능) / `assoc`(연관, 가는 실선) / `oppo`(반대, 점선).
   - §3의 강도축·양가·반대 쌍을 데이터로.
3. **렌더링**
   - `renderEdges`를 타입별 스타일로(계층=실선, 연관=가는 실선, 반대=점선). CSS 클래스 추가.
   - 레이아웃: 방사형 트리(감정→4분류→leaf)로 위치만 잡고 연관/반대는 오버레이. 겹침-해소 패스 활용.
4. **게이팅(핵심 루프)**
   - 4대분류 = 중간 노드 → 걸음으로 분류 도달 후 그 아래 감정 펼침.
   - 연관/반대 엣지 역할은 결정 5 참조.
5. **유지되는 시스템** — 프레스티지·로그 재해석·세이브·지도 필터(감정/몸)·모달은 leaf를 `type:"person"`으로 두면 거의 무수정.
6. **문서** — `CLAUDE.md` §5 감정 캐넌(6계열·C&K27)을 새 4분류 체계로 갱신.

---

## 5. 확정해야 할 결정 (코딩 전 필수)

말씀한 목록이 기존 게임의 핵심 서사와 충돌하는 지점이 있어 정리:

1. **개수: 새 목록은 26개.** 27로 맞출지(하나 추가) / 26으로 갈지.
2. **🔴 '슬픔(Sadness)'이 새 목록에 없음.** 엔딩이 *"슬픔을 알고, 기쁨을 알고…"* 로 슬픔에 의존. → `괴로움/아련함`으로 대체할지, 슬픔을 추가할지.
3. **🔴 '해냄(Triumph)→샘 재회' 게이트가 사라짐.** 엔딩 도달 조건을 **"감정 전부 + 몸 전부 수집"** 으로 변경할지(추천).
4. **연관/반대 엣지 = 시각 전용 vs 이동 경로?** **추천: 시각 전용**(관계선만). 이동은 계층 엣지로만 — 핵심 루프 유지, 지형도만 입체화.
5. **'성적 욕구(Desire)'** — 기존 SFW 순화('끌림/연정') 유지 추천(스토어 심사·톤). 그대로 둘지.
6. **일기 콘텐츠** — 바뀐/새 감정 사연·일기는 톤 맞춰 새로 작성(에이전트가).
7. **몸(11 기관계) 트랙** — 이번 개편은 감정만. 몸 지도는 그대로 유지.

### 추천 디폴트 (이대로면 바로 구현 착수)
> 27개로 맞추되 **슬픔 추가** · **해냄 대신 "전부 수집=엔딩"** · **연관/반대는 시각 전용** · **성적 욕구는 '끌림'으로 순화** · **콘텐츠 새로 작성** · **몸 트랙 유지**.

---

## 6. 참고 — 현재 구현된 감정 27 (교체 대상, C&K 6계열)
애정·아름다움(애틋함·아름다움·감탄·끌림) / 밝음(기쁨·재미·신남·설렘) / 시림(슬픔·그리움·갈망·안쓰러움·연민) / 무거움-불편(두려움·불안·민망함·혐오·오싹함) / 관조(호기심·경외·황홀·평온) / 단단함→길끝(혼란·지루함·안도·뿌듯함·해냄).
